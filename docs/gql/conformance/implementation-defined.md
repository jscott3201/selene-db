<!-- @generated by selene-profile; generator-version: 3; profile-hash: 9fdc36c57dad00a597c810c5b99a0695064eef9d7eb7ce42d30c3ce0e221e6d8 -->

# GQL implementation-defined profile

This report is a complete ID inventory and implementation audit aid for the selected Selene profile. It paraphrases Selene decisions only; Annex B remains in the standard. This report is not normative text and does not make a conformance claim.

Profile: `selene-gql-core-2.0`. Release claimable: **false**.

| ID | Topic | Applies | Disposition or value | Rationale | Evidence |
|---|---|---|---|---|---|
| IA001 | Result declared-type exposure | yes (`APP-ALWAYS`) | selected: true; stable; public | Row results expose analyzer-inferred column types through the public binding-table schema. | `EVID-RESULT-DESCRIPTORS` |
| IA002 | Additional status chaining | yes (`APP-ALWAYS`) | pending: M03-PR03 | The current error API has one typed error and does not yet model a status-object chain. | `EVID-PLAN-M03-PR03` |
| IA003 | Operations on unnormalized strings | yes (`APP-STRING`) | pending: M05-PR03 | Normalization behavior is implemented per operation but has no reviewed profile-wide rule. | `EVID-PLAN-M05-PR03` |
| IA004 | Approximate value interpretation | yes (`APP-IEEE-FLOAT`) | selected: FLOAT32 uses IEEE 754 binary32; FLOAT and FLOAT64 use IEEE 754 binary64; stable; public | Parsed and stored approximate values use Rust f32 and f64 representations. | `EVID-NUMERIC-TYPES` |
| IA005 | Assignment digit-loss rule | yes (`APP-NUMERIC`) | pending: M05-PR03 | The type-system work item must unify assignment conversion and precision-loss rules. | `EVID-PLAN-M05-PR03` |
| IA006 | Choice among numeric approximations | yes (`APP-NUMERIC`) | pending: M05-PR03 | Numeric coercion needs one reviewed rule across literals, operators, casts, and assignment. | `EVID-PLAN-M05-PR03` |
| IA007 | Non-exact values with approximations | yes (`APP-NUMERIC`) | pending: M05-PR03 | The structural type model will define which non-exact value families admit approximations. | `EVID-PLAN-M05-PR03` |
| IA010 | Arithmetic operating bounds | yes (`APP-NUMERIC`) | pending: M05-PR03 | Arithmetic bounds are spread across native integer, decimal, and floating representations. | `EVID-PLAN-M05-PR03` |
| IA011 | Approximate division digit loss | yes (`APP-IEEE-FLOAT`) | pending: M05-PR03 | Division conversion policy must be selected with the unified numeric type rules. | `EVID-PLAN-M05-PR03` |
| IA012 | Flagger treatment of implementation-defined items | yes (`APP-ALWAYS`) | selected: false; provisional; public | The current Flagger reports optional feature IDs and does not emit Annex B decisions. | `EVID-FLAGGER` |
| IA013 | Selective-path exception termination | yes (`APP-SELECTIVE-PATHS`) | selected: true; stable; internal | Selective path evaluation propagates an executor error instead of continuing with partial rows. | `EVID-PATH-ERRORS` |
| IA014 | Context-free NULL type error | yes (`APP-ALWAYS`) | selected: false; stable; public | A context-free NULL expression retains the dedicated NULL type without raising an exception. | `EVID-NULL-TYPE` |
| IA015 | Character comparison padding | yes (`APP-STRING`) | selected: false; stable; public | String comparison is lexicographic over the stored scalar sequence without right padding. | `EVID-VALUE-COMPARE` |
| IA016 | Byte comparison zero padding | yes (`APP-BYTE-STRINGS`) | selected: false; stable; public | Byte strings compare as exact byte slices without ignoring trailing zero bytes. | `EVID-VALUE-COMPARE` |
| IA017 | Repeated property assignments | yes (`APP-UPDATES`) | pending: M06-PR05 | The batch mutation operator will select and test one rule for repeated assignments. | `EVID-PLAN-M06-PR05` |
| IA019 | Bidirectional controls in literals | yes (`APP-STRING`) | pending: M05-PR01 | The source-syntax boundary needs an explicit security rule for bidirectional controls. | `EVID-PLAN-M05-PR01` |
| IA020 | Private-use characters in identifiers | yes (`APP-ALWAYS`) | selected: false; stable; public | Catalog name validation rejects every General Category Co scalar in regular and delimited identifiers. | `EVID-CATALOG-DOCS`, `EVID-CATALOG-NAME-SOURCE`, `EVID-CATALOG-TESTS`, `EVID-PLAN-M02-PR02` |
| IA021 | Numeric assignment overflow handling | yes (`APP-NUMERIC`) | pending: M05-PR03 | The structural type model must align assignment overflow across every numeric family. | `EVID-PLAN-M05-PR03` |
| IA023 | Source newline code | yes (`APP-ALWAYS`) | selected: U+000A; stable; public | The parser and source spans use line feed as the source newline separator. | `EVID-PARSER-SOURCE` |
| IA025 | IEEE additional-value processing | yes (`APP-IEEE-FLOAT`) | selected: unordered_nan_predicates_total_sort_order; stable; public | Predicate comparison preserves IEEE NaN unorderedness while deterministic sorting uses total order. | `EVID-VALUE-COMPARE` |
| IA026 | Leap-second and calendar discontinuity support | yes (`APP-TEMPORAL`) | selected: false; stable; public | Temporal parsing accepts the jiff civil-time range and does not admit leap-second images. | `EVID-TEMPORAL-PARSE` |
| ID001 | Principal representation | yes (`APP-ALWAYS`) | selected: embedder_supplied_opaque_bytes; stable; embedder | The embedder optionally supplies opaque principal bytes that are forwarded to commits. | `EVID-SESSION-PRINCIPAL` |
| ID002 | Principal home schema and graph | yes (`APP-ALWAYS`) | pending: M03-PR01 | SessionContext will own principal defaults after the catalog-first facade exists. | `EVID-PLAN-M03-PR01` |
| ID003 | Authorization privileges | yes (`APP-ALWAYS`) | pending: M03-PR01 | The embedded principal contract does not yet include an authorization privilege model. | `EVID-PLAN-M03-PR01` |
| ID004 | Constructed-value empty element types | yes (`APP-CONSTRUCTED`) | pending: M05-PR03 | Structural type descriptors will define element types when no concrete element is present. | `EVID-PLAN-M05-PR03` |
| ID005 | Elements-function declared type | yes (`APP-CONSTRUCTED`) | pending: M05-PR03 | Function result descriptors must be derived from the structural type model. | `EVID-PLAN-M05-PR03` |
| ID006 | Default transaction characteristics | yes (`APP-TRANSACTIONS`) | selected: READ_WRITE; SERIALIZABLE; provisional; public | The engine uses serialized writes and immutable read snapshots; no weaker isolation mode is exposed. | `EVID-TRANSACTIONS` |
| ID016 | Condition-text language | yes (`APP-ALWAYS`) | selected: English source messages; stable; public | Typed errors carry checked-in English messages and no localization service. | `EVID-DIAGNOSTICS` |
| ID017 | Diagnostic information map | yes (`APP-ALWAYS`) | selected: GQLSTATUS; source_span; typed_error_fields; stable; public | Errors expose GQLSTATUS plus typed fields and source spans where available. | `EVID-DIAGNOSTICS` |
| ID022 | Default collation | yes (`APP-STRING`) | selected: unicode_scalar_lexicographic; stable; public | Character strings compare lexicographically by their stored Rust string scalar sequence. | `EVID-VALUE-COMPARE` |
| ID023 | Preferred string type names | yes (`APP-STRING`) | selected: STRING; BYTES; stable; public | Formatting uses STRING for character strings and BYTES for byte strings. | `EVID-TYPE-NAMES` |
| ID028 | Integer effective binary precision | yes (`APP-NUMERIC`) | selected: INT8 and UINT8: 8; INT16, SMALLINT, UINT16, and USMALLINT: 16; INT32 and UINT: 32; INTEGER, INT64, BIGINT, UINT64, and UBIGINT: 64; INT128 and UINT128: 128; stable; public | The analyzer maps supported integer names to fixed Rust integer widths. | `EVID-NUMERIC-TYPES` |
| ID034 | Decimal effective precision | yes (`APP-DECIMAL`) | selected: 29; stable; public | Decimal type metadata rejects precision above MAX_DECIMAL_PRECISION 29. | `EVID-DECIMAL-LIMITS` |
| ID037 | Approximate numeric precision and scale | yes (`APP-IEEE-FLOAT`) | selected: FLOAT32 and REAL: binary precision 24, variable scale; FLOAT, FLOAT64, and DOUBLE: binary precision 53, variable scale; stable; public | Approximate types map to IEEE binary32 or binary64 storage; this is the relocated float-width policy. | `EVID-NUMERIC-TYPES` |
| ID048 | Default time-zone displacement | yes (`APP-TEMPORAL`) | selected: UTC+00:00; stable; public | A session without an override evaluates temporal functions in UTC and reset restores UTC. | `EVID-SESSION-DEFAULTS` |
| ID049 | Default session parameters | yes (`APP-SESSION-PARAMETERS`) | selected: 0; stable; public | New sessions start with empty graph, table, and scalar parameter maps. | `EVID-SESSION-DEFAULTS` |
| ID057 | List ordinality result type | yes (`APP-LISTS`) | selected: INTEGER; stable; public | FOR ordinality values are represented as non-negative Value::Int entries. | `EVID-ORDINALITY` |
| ID058 | List offset result type | yes (`APP-LISTS`) | selected: INTEGER; stable; public | FOR offset values are represented as non-negative Value::Int entries. | `EVID-ORDINALITY` |
| ID059 | COUNT result type | yes (`APP-AGGREGATES`) | selected: INTEGER; stable; public | The analyzer assigns INTEGER to COUNT expressions and the runtime returns Value::Int. | `EVID-AGGREGATES` |
| ID061 | SESSION_USER declared type | yes (`APP-ALWAYS`) | pending: M03-PR01 | The future principal and session context must define the exposed SESSION_USER representation. | `EVID-PLAN-M03-PR01` |
| ID062 | Non-negative integer specification type | yes (`APP-NUMERIC`) | pending: M05-PR03 | Syntax fields use several host widths and need one structural declared type. | `EVID-PLAN-M05-PR03` |
| ID063 | Mixed approximate arithmetic result type | yes (`APP-IEEE-FLOAT`) | pending: M05-PR03 | Current promotion is implemented but needs review against every approximate operand pairing. | `EVID-PLAN-M05-PR03` |
| ID064 | Exact arithmetic result type | yes (`APP-NUMERIC`) | pending: M05-PR03 | Exact integer and decimal promotion needs one descriptor-level rule. | `EVID-PLAN-M05-PR03` |
| ID065 | Exact addition and subtraction precision | yes (`APP-NUMERIC`) | pending: M05-PR03 | Precision derivation will be fixed with structural numeric descriptors. | `EVID-PLAN-M05-PR03` |
| ID066 | Exact multiplication precision | yes (`APP-NUMERIC`) | pending: M05-PR03 | Precision derivation will be fixed with structural numeric descriptors. | `EVID-PLAN-M05-PR03` |
| ID067 | Exact division precision and scale | yes (`APP-NUMERIC`) | pending: M05-PR03 | Division precision and scale need one checked descriptor rule. | `EVID-PLAN-M05-PR03` |
| ID068 | Length-expression result type | yes (`APP-CONSTRUCTED`) | selected: INTEGER; stable; public | Character, byte, and path length functions produce non-negative Value::Int results. | `EVID-LENGTH-FUNCTIONS` |
| ID069 | Advanced numeric function result types | yes (`APP-IEEE-FLOAT`) | selected: FLOAT; stable; public | Trigonometric, logarithmic, exponential, and power functions return binary64 Value::Float values. | `EVID-NUMERIC-FUNCTIONS` |
| ID070 | Cardinality result type | yes (`APP-CONSTRUCTED`) | selected: INTEGER; stable; public | CARDINALITY and SIZE produce non-negative Value::Int results. | `EVID-LENGTH-FUNCTIONS` |
| ID074 | Exact expression result precision | yes (`APP-NUMERIC`) | pending: M05-PR03 | Expression-wide exact precision needs the structural numeric descriptor implementation. | `EVID-PLAN-M05-PR03` |
| ID075 | Approximate expression result precision | yes (`APP-IEEE-FLOAT`) | pending: M05-PR03 | Expression-wide approximate precision needs the structural numeric descriptor implementation. | `EVID-PLAN-M05-PR03` |
| ID076 | ELEMENT_ID result type | yes (`APP-ELEMENT-ID`) | selected: STRING; stable; public | ELEMENT_ID converts stable element identifiers to database strings. | `EVID-ELEMENT-ID` |
| ID079 | Approximate literal declared type | yes (`APP-IEEE-FLOAT`) | selected: FLOAT; stable; public | Unsuffixed approximate literals are parsed as binary64 FLOAT values. | `EVID-NUMERIC-LITERALS` |
| ID085 | Context-free NULL declared type | yes (`APP-ALWAYS`) | selected: NULL; stable; public | The analyzer retains GqlType::Null when no surrounding context supplies another type. | `EVID-NULL-TYPE` |
| ID086 | Default graph pattern match mode | yes (`APP-ALWAYS`) | selected: REPEATABLE ELEMENTS; stable; public | A MATCH without a mode installs no edge-uniqueness filter and matches explicit REPEATABLE ELEMENTS. | `EVID-MATCH-MODE` |
| ID089 | Preferred graph type name | yes (`APP-GRAPH-TYPES`) | selected: GRAPH; provisional; public | The formatter and type vocabulary use GRAPH rather than PROPERTY GRAPH as the preferred form. | `EVID-TYPE-NAMES` |
| ID090 | Preferred node type name | yes (`APP-ALWAYS`) | selected: NODE; stable; public | The public type vocabulary and value variants use NODE terminology. | `EVID-TYPE-NAMES` |
| ID091 | Preferred edge type name | yes (`APP-ALWAYS`) | selected: EDGE; stable; public | The public type vocabulary and value variants use EDGE terminology. | `EVID-TYPE-NAMES` |
| ID095 | Exact SUM result type | yes (`APP-AGGREGATES`) | pending: M05-PR03 | Aggregate exact-result descriptors need the unified numeric promotion contract. | `EVID-PLAN-M05-PR03` |
| ID096 | Exact AVG result type | yes (`APP-AGGREGATES`) | pending: M05-PR03 | Aggregate exact-result descriptors need the unified numeric promotion contract. | `EVID-PLAN-M05-PR03` |
| ID097 | Approximate SUM and AVG result types | yes (`APP-AGGREGATES`) | pending: M05-PR03 | Aggregate approximate-result descriptors need the unified numeric promotion contract. | `EVID-PLAN-M05-PR03` |
| ID098 | Standard-deviation result types | yes (`APP-AGGREGATES`) | pending: M05-PR03 | Statistical aggregate descriptors need the unified numeric promotion contract. | `EVID-PLAN-M05-PR03` |
| ID099 | Binary aggregate result types | yes (`APP-AGGREGATES`) | pending: M05-PR03 | Binary aggregate descriptors need the unified numeric promotion contract. | `EVID-PLAN-M05-PR03` |
| IE001 | URI and URL resource mapping | yes (`APP-ALWAYS`) | pending: M02-PR01 | The embedded facade has not selected a catalog resource mapping for URI or URL values. | `EVID-PLAN-M02-PR01` |
| IE002 | Transaction isolation selection | yes (`APP-TRANSACTIONS`) | selected: SERIALIZABLE; provisional; public | No isolation selector is exposed; writes serialize and readers observe immutable snapshots. | `EVID-TRANSACTIONS` |
| IE003 | Unicode identifier profile | yes (`APP-ALWAYS`) | selected: Unicode data version 17.0.0; UAX #31 R1-2 profile of R1-1: XID_Start/XID_Continue plus U+005F LOW LINE at start and continuation; delimited identifiers use decoded spelling outside the XID repertoire; stable; public | Pinned unicode-ident and unicode-normalization data define the catalog identifier and normalization boundary. | `EVID-CATALOG-DOCS`, `EVID-CATALOG-NAME-SOURCE`, `EVID-CATALOG-TESTS`, `EVID-PLAN-M02-PR02` |
| IE004 | Serializable behavior relaxations | yes (`APP-TRANSACTIONS`) | selected: false; provisional; public | The current transaction API documents no relaxation from serialized writes and snapshot reads. | `EVID-TRANSACTIONS` |
| IE005 | Nonconforming source treatment | yes (`APP-ALWAYS`) | selected: reject_with_typed_syntax_error; stable; public | Source outside the grammar is rejected by the parser with a typed diagnostic. | `EVID-PARSER-SOURCE` |
| IE006 | Mixed-mode transaction restrictions | no (`APP-MIXED-TRANSACTIONS`) | not applicable | GP18 is absent from the selected closure, so catalog-and-data statement mixing is not in this profile. | `EVID-PROFILE-GP18` |
| IE007 | Mixed-mode violation conditions | no (`APP-MIXED-TRANSACTIONS`) | not applicable | GP18 is absent from the selected closure, so no mixed-mode violation condition is selected. | `EVID-PROFILE-GP18` |
| IE008 | Additional warning conditions | yes (`APP-ALWAYS`) | pending: M05-PR06 | Unified compiler diagnostics will define any implementation-added warnings. | `EVID-PLAN-M05-PR06` |
| IE009 | Additional informational conditions | yes (`APP-ALWAYS`) | pending: M05-PR06 | Unified diagnostics will define any implementation-added informational conditions. | `EVID-PLAN-M05-PR06` |
| IE010 | Successful informational subclasses | yes (`APP-ALWAYS`) | pending: M05-PR06 | The status registry must select successful informational subclasses as one closed set. | `EVID-PLAN-M05-PR06` |
| IL001 | Graph-element label-set cardinality | yes (`APP-ALWAYS`) | selected: nodes: minimum 0, no fixed profile maximum; edges: minimum 1, maximum 1; stable; public | Node LabelSet accepts empty or multiple labels; EdgeTypeDef has exactly one discriminator label. | `EVID-LABEL-SETS` |
| IL002 | Graph-element property-set maximum | yes (`APP-ALWAYS`) | selected: 4294967295; stable; public | PropertyMap rejects entry counts above the u32 maximum for node and edge properties. | `EVID-PROPERTY-LIMIT` |
| IL003 | Key label-set cardinality | yes (`APP-KEY-LABELS`) | selected: node key labels: minimum 1, maximum 1; edge key labels: minimum 1, maximum 1; provisional; public | Current key-label lowering and storage admit singleton key label sets only. | `EVID-CAPS` |
| IL009 | Variable string-concatenation minimum | yes (`APP-STRING`) | selected: 0; stable; public | Concatenating two empty variable-length strings yields an empty string. | `EVID-STRING-CONCAT` |
| IL010 | Unsigned integer literal digit maximum | yes (`APP-NUMERIC`) | selected: 19; stable; public | Production int_lit parsing delegates to parse_i64, so the largest accepted unsigned decimal image is i64::MAX, which has 19 digits. | `EVID-NUMERIC-LITERALS` |
| IL011 | Numeric precision and scale maxima | yes (`APP-NUMERIC`) | selected: binary integer precision: 128; decimal precision: 29; decimal scale: 28; approximate binary precision: 53; stable; public | These bounds are enforced by the supported native integer, decimal, and float representations. | `EVID-DECIMAL-LIMITS`, `EVID-NUMERIC-TYPES` |
| IL013 | String value length maxima | yes (`APP-STRING`) | selected: stored STRING bytes: 4294967295; default STRING concatenation characters: 4294967295; default BYTES concatenation bytes: 4294967295; stable; embedder | DbString and the default concatenation caps use the u32 maximum. | `EVID-CAPS`, `EVID-STRING-LIMIT` |
| IL015 | Constructed value cardinality maxima | yes (`APP-CONSTRUCTED`) | selected: property maps: 4294967295 entries; constructed lists: 1000000 entries by default; paths: 32 edges by default; stable; embedder | PropertyMap and session implementation-defined caps enforce these current bounds. | `EVID-CAPS`, `EVID-PROPERTY-LIMIT` |
| IL018 | General quantifier upper bound | yes (`APP-PATHS`) | selected: 100; stable; embedder | The default planner cap rejects larger variable-length quantifier upper bounds. | `EVID-CAPS` |
| IL020 | Directory nesting depth | yes (`APP-CATALOG`) | selected: 0; provisional; public | The finalized 2.0 catalog decision selects one synthetic root with schemas and no child directories. | `EVID-CATALOG-DECISION` |
| IL023 | Approximate exponent bounds | yes (`APP-IEEE-FLOAT`) | pending: M05-PR03 | The structural numeric model must state normal and subnormal exponent bounds consistently. | `EVID-PLAN-M05-PR03` |
| IL024 | Fractional-seconds precision maximum | yes (`APP-TEMPORAL`) | selected: 9; stable; public | Temporal values and parsing use nanosecond fractional precision. | `EVID-TEMPORAL-PARSE` |
| IS001 | Implicit NULL ordering | yes (`APP-ALWAYS`) | selected: NULLS HIGH; stable; public | Omitted NULLS ordering puts NULL last for ascending order and first for descending order. | `EVID-NULL-SORT` |
| IV001 | GQL source character repertoire | yes (`APP-ALWAYS`) | selected: Unicode scalar values in UTF-8; stable; public | Rust source strings are valid UTF-8; this selection does not claim an exact Unicode data version. | `EVID-PARSER-SOURCE` |
| IV002 | Otherwise-unspecified essential inequality | yes (`APP-ALWAYS`) | pending: M05-PR03 | The structural comparison matrix must close every essentially comparable pairing. | `EVID-PLAN-M05-PR03` |
| IV003 | GQL object normal forms | yes (`APP-CATALOG`) | selected: catalog_descriptor; directory_descriptor; schema_descriptor; graph_descriptor; graph_type_descriptor; binding_table_descriptor; procedure_descriptor; index_descriptor; constraint_descriptor; provisional; public | Each supported catalog object has one immutable kind-tagged descriptor with typed identity, name, parent, generation, creation metadata, and a storage-neutral payload. | `EVID-CATALOG-DESCRIPTOR-SOURCE`, `EVID-CATALOG-DOCS`, `EVID-CATALOG-TESTS`, `EVID-PLAN-M02-PR02` |
| IV008 | Value type normal forms | yes (`APP-ALWAYS`) | pending: M05-PR03 | Structural type descriptors will define one normal form for every supported value type. | `EVID-PLAN-M05-PR03` |
| IV010 | Universal comparison result | yes (`APP-ALWAYS`) | pending: M05-PR03 | Predicate and sort comparison currently have different fallback domains and need one reviewed rule. | `EVID-PLAN-M05-PR03` |
| IV011 | Dynamic property value type | yes (`APP-DYNAMIC-UNION`) | selected: NULL; BOOLEAN; INTEGER; UNSIGNED INTEGER; DECIMAL; FLOAT32; FLOAT64; STRING; BYTES; DATE; TIME; DATETIME; DURATION; LIST; RECORD; NODE; EDGE; PATH; UUID; JSON; VECTOR; provisional; public | Property values use the closed native Value family, including selected implementation extensions. | `EVID-VALUE-TYPES` |
| IV012 | Open dynamic union components | yes (`APP-DYNAMIC-UNION`) | pending: M05-PR03 | The target selects open unions but the current analyzer marks their ISO feature unsupported. | `EVID-PLAN-M05-PR03` |
| IV014 | Static-type covering set | yes (`APP-ALWAYS`) | selected: ANY; provisional; public | GqlType::Any is the broad static value supertype used by unconstrained procedure surfaces. | `EVID-VALUE-TYPES` |
| IV015 | Authorization identifier syntax | yes (`APP-ALWAYS`) | pending: M03-PR01 | Opaque principal bytes are accepted, but no GQL authorization identifier syntax is selected. | `EVID-PLAN-M03-PR01` |
| IV016 | Additional condition text | yes (`APP-ALWAYS`) | selected: typed English diagnostic message; stable; public | Each typed error supplies concise implementation text in addition to its status and fields. | `EVID-DIAGNOSTICS` |
| IV023 | Truncating whitespace set | yes (`APP-STRING`) | selected: U+0020; stable; public | Shared string coercion and concatenation discard only trailing space when truncation is permitted. | `EVID-TRUNCATING-WHITESPACE` |
| IW001 | Embedded session and request mechanism | yes (`APP-ALWAYS`) | selected: Session::new; Session::with_principal; Session::execute_source; provisional; embedder | The in-process embedder creates and drops Session values and submits requests through Rust methods. | `EVID-SESSION-API` |
| IW002 | Authorization identifier lifecycle | yes (`APP-ALWAYS`) | selected: embedder maps principals to opaque bytes; provisional; embedder | Selene stores no user directory; the embedder owns identity lifecycle and supplies principal bytes. | `EVID-SESSION-PRINCIPAL` |
| IW003 | Last-request determination | yes (`APP-ALWAYS`) | selected: SESSION CLOSE or Session drop; provisional; embedder | The caller ends the in-process session explicitly or by dropping its Session value. | `EVID-SESSION-API` |
| IW004 | Alternative transaction control mechanism | yes (`APP-TRANSACTIONS`) | selected: SharedGraph::begin_write; WriteTxn::commit; WriteTxn drop rollback; provisional; embedder | Rust embedders can control graph write transactions without issuing transaction statements. | `EVID-TRANSACTIONS` |
| IW005 | Termination status delivery | yes (`APP-ALWAYS`) | selected: Rust Result with typed success or error; stable; embedder | Session and transaction methods return typed Result values directly to the embedder. | `EVID-DIAGNOSTICS`, `EVID-TRANSACTIONS` |
| IW006 | Request parameter dictionary | yes (`APP-SESSION-PARAMETERS`) | selected: Session parameter maps plus request bindings; provisional; embedder | Session owns named typed parameter maps that seed each request context. | `EVID-SESSION-DEFAULTS` |
| IW007 | Status presentation | yes (`APP-ALWAYS`) | selected: typed Rust error; GQLSTATUS; miette source diagnostic; stable; public | Callers receive typed errors and can render source-aware diagnostics without parsing display text. | `EVID-DIAGNOSTICS` |
| IW010 | External procedure provisioning | yes (`APP-NAMED-PROCEDURES`) | selected: native BuiltinProcedureRegistry; stable; public | Production uses the frozen in-tree registry; there is no loadable extension mechanism. | `EVID-PROCEDURE-REGISTRY` |
| IW011 | Pattern element reference type resolution | yes (`APP-ALWAYS`) | selected: analyzer binding and graph schema; provisional; internal | Pattern binding types are inferred from node or edge roles and available graph schema metadata. | `EVID-PATTERN-TYPES` |
| IW012 | Inserted-node reference type resolution | yes (`APP-UPDATES`) | selected: analyzer binding and graph schema; provisional; internal | Insert-node bindings derive their reference type from the analyzed insert pattern and schema. | `EVID-PATTERN-TYPES` |
| IW014 | Visual-confusable string detection | yes (`APP-STRING`) | selected: none; stable; public | The engine performs exact scalar comparison and does not classify visual confusability. | `EVID-VALUE-COMPARE` |
| IW015 | Automatic directory creation | yes (`APP-CATALOG`) | selected: none; provisional; public | The selected flat catalog has one synthetic root and does not auto-create child directories. | `EVID-CATALOG-DECISION` |
| IW016 | Automatic schema population | yes (`APP-CATALOG`) | selected: none; provisional; public | Schema objects are created explicitly; the catalog plan selects no automatic population. | `EVID-CATALOG-DECISION` |
| IW017 | Concatenation of non-normalized strings | yes (`APP-STRING`) | selected: concatenate unchanged unless both operands are NFC; stable; public | Concatenation normalizes the boundary to NFC only when both inputs are already NFC. | `EVID-STRING-CONCAT` |
| IW018 | Lax-cast generation | yes (`APP-CASTS`) | pending: M05-PR03 | The structural cast matrix must define lax-cast generation and companion type tests together. | `EVID-PLAN-M05-PR03` |
| IW019 | Common-supertype selection | yes (`APP-ALWAYS`) | selected: analyzer numeric promotion and structural family matching; provisional; internal | The analyzer computes common numeric types with a closed promotion function. | `EVID-NUMERIC-PROMOTION` |
| IW021 | Type-precedence permutation | yes (`APP-ALWAYS`) | pending: M05-PR03 | The structural type system must publish one complete precedence order for common-type selection. | `EVID-PLAN-M05-PR03` |
| IW022 | Non-null assignment proof | yes (`APP-UPDATES`) | selected: runtime value validation before mutation publication; stable; internal | Assignments validate nullability against the target type before a transaction can commit. | `EVID-NULLABILITY` |
| IW023 | Canonical identifier name | yes (`APP-CATALOG`) | selected: NFC of decoded spelling; case-sensitive without case folding; Unicode scalar value lexicographic ordering; stable; public | Catalog dictionaries compare NFC spellings by Unicode scalar value while descriptors retain decoded display spelling. | `EVID-CATALOG-DOCS`, `EVID-CATALOG-NAME-SOURCE`, `EVID-CATALOG-TESTS`, `EVID-PLAN-M02-PR02` |
| IW025 | Catalog procedure transaction control | yes (`APP-CATALOG`) | selected: one graph write transaction per current catalog statement; provisional; public | Current catalog operations execute through the write transaction and publish only at commit. | `EVID-CATALOG-TRANSACTIONS` |
