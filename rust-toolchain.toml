# Pinned to latest stable at scaffold time. Bump tracked in repo CHANGELOG.
# Per the engineering discipline floor (see CLAUDE.md hard rules), every
# selene-db build pins this file at the workspace root.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
profile = "default"
