-- corpus: positive
-- feature: GF01
-- expects: parse-ok
RETURN abs(-3) AS absolute_value,
       mod(7, 4) AS remainder,
       floor(1.8) AS floored,
       ceil(1.2) AS ceiled,
       ceiling(1.2) AS ceiling_alias,
       sqrt(9) AS square_root
