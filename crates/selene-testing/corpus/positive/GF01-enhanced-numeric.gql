// corpus: positive
// feature: GF01
// expects: parse-ok
RETURN abs(-3) AS absolute_value,
       mod(7, 4) AS remainder,
       floor(1.8D) AS floored,
       ceil(1.2D) AS ceiled,
       ceiling(1.2D) AS ceiling_alias,
       sqrt(9) AS square_root
