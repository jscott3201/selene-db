# selene-db — repo guide for Claude

## Mission

`selene-db` is a strict ISO/IEC 39075:2024 GQL property graph engine, designed greenfield to keep the core small and high-performance and to move every non-graph capability (time-series, vectors, RDF, GraphRAG, etc.) into a clean extension/module system.

This is a **marathon, not a sprint**. No shortcuts. Every decision should optimize for correctness, performance, and a stable extension contract — even when that means more work today.

## Status

Active implementation. Architecture is settled (D1–D19, see Decision log). Workspace contains **6 of 7 v1.0 mandatory crates**: `selene-core`, `selene-graph`, `selene-persist`, `selene-gql`, `selene-testing`, `selene-pack` (opened 2026-05-10 with BRIEF-41). `selene-algorithms` remains to be built. **M5d (executor) closed 2026-05-10**; **M5e (procedure-pack registry) opened 2026-05-10**, currently 1/9 briefs merged. See Milestone log below.

## North star: ISO/IEC 39075:2024

The ISO GQL spec PDF lives at `_spec/ISO_GQL/ISO_IEC_39075_2024(en).pdf` (~588 pages). Conformance is the language-level contract; everything else (transport, auth, extensions, isolation) is the implementation's choice.

Key facts:

- **One mandatory tier ("minimum conformance") + 228 optional features** (à la carte, not tiered profiles). Implications table (≈70 pairs) shows which features auto-imply others.
- **Mandatory data types are only `STRING / BOOLEAN / INT / FLOAT`.** Everything else is optional.
- **At least one of GG01 (open graph) or GG02 (closed graph) is required.**
- **Default isolation is serializable** (clause 4.6); relaxations are impl-defined (`IE004`).
- **No wire format is in the spec** (clause 4.2.3 is explicit).
- **Vectors, time-series, graph algorithms, auth — all out of spec.**
- Extension hooks the spec gives us: `IW010` (external procedures via normative `CALL`), `IV011` (dynamic property value type), `ID001`/`IW002`/`ID003` (impl-defined principals/authzn/privileges), `IE002`/`IE004` (transaction isolation).
- **GQL Flagger (clause 24.6) is required** because we ship optional features — non-standard constructs must syntactically flag at parse time. The canonical feature surface is `crates/selene-core/src/feature_register.rs::SUPPORTED_FEATURES`.

## Hard rules

1. **Strict ISO GQL compliance.** Anything not in the spec lives in extensions or impl-defined hooks. Never extend the GQL grammar itself.
2. **No shortcuts.** No "phase X scaffold," no placeholder framing, no TODO-it-later code. Documents describe the layer the type ships, not the order of sprints.
3. **`#![forbid(unsafe_code)]` workspace-wide.** Bounded RFC-specified surfaces may be exceptions, with justification in `// Why:` comments.
4. **`missing_docs = "deny"` workspace-wide.** Every `pub` item carries a rustdoc with intent, invariants, or non-obvious behavior. Dead surfaces get demoted to `pub(crate)`, not stubbed.
5. **File-size cap: 700 LOC.** Refactor or split before approaching the cap. Enforced by `.github/scripts/check-file-size.sh`. **This is the only LOC gate.**
6. **Per-crate LOC guidance (NOT merge-gating):** rough budgets are `selene-core` ~3K; `selene-graph` ~8K; `selene-gql` ~35–45K; `selene-persist` ~5K; `selene-pack` ~10K; `selene-algorithms` ~5K; `selene-testing` ~2K. These are structural hints for *when to split into a new module/extension crate* (e.g., HNSW belongs in selene-vector, not selene-graph). They are NOT acceptance-bar gates — do not write them into brief checklists or burn implementation time shaving lines to hit them.
7. **rustls-only TLS posture.** No native-tls, no openssl-sys. Enforced in `deny.toml`.
8. **Dual MIT OR Apache-2.0 license.** Permissive transitive license allow-list per `deny.toml` (with one explicit `MPL-2.0` exception for `imbl`, see D7).
9. **Latest stable Rust.** Pinned in `rust-toolchain.toml`. Latest published deps with strong adoption signals.
10. **Never hand-roll crypto / TLS / async runtime / serialization primitives.**
11. **Every new I/O surface routes through a single mutation/auth funnel — enforced by types.** No "this new endpoint forgot to go through the funnel" class of bug.
12. **GQL is the sole query and mutation interface.** No SQL/Cypher/SPARQL grammar in the engine; if needed, ship as extensions that translate to GQL or call procedures.
13. **Attribution.** Donor codebases (SeleneDB, AetherDB, aether-db, rusty-bacnet) are the user's own work and need no per-line attribution. Third-party code (crates, blog posts, vendored snippets) does:
    - `NOTICE` — Apache-2.0-style notice naming third-party copyright holders for bundled/adapted code.
    - `THIRDPARTY.md` — auto-generated from `Cargo.lock` via `cargo-about`; CI-gated against drift.
    - **Per-file attribution** for non-trivial adapted third-party code: `// Adapted from <upstream>@<version-or-commit> (<SPDX>) — <brief note>`.
    - Spec derivation: cite ISO clause numbers in `// Why:` comments (`// Per ISO 39075:2024 clause 16.4`).

## Forward-looking principles (from donor archaeology)

- **Make the extension boundary first-class on day one.** SeleneDB's `Procedure::execute(...hot_tier: Option<&HotTier>...)` was the most damaging coupling — every procedure carried a TS reference because the trait was designed when TS was first-class. The fix is **per-tier `Context` types + per-tier registries** (D17).
- **Refuse to put auxiliary subsystems inside core.** HNSW lived inside `selene-graph` and that's why "core" grew to 19 KLOC. Vectors, time-series, RDF, GraphRAG all live as extensions (D5).
- **Two-stage validation with explicit coverage struct** (the aether-db pattern). Make "what we check today" vs "what the spec demands" a programmatic value, not a comment.
- **Typestate-sealed registry transitions + atomic graph commit + post-commit audit.** Audit lag is recoverable; fictional audit is not.

## Crate map

v1.0 mandatory (every consumer pulls these), with dependency direction:

| Crate | Depends on | Owns |
|---|---|---|
| `selene-core` | none | Foundation types: `Value` (4 mandatory ISO types + `Value::Extended`), `IStr`, `PropertyMap`, `LabelSet`, schema types, `Codec`, `Origin`, `Changeset`. |
| `selene-graph` | core | In-memory property graph: ArcSwap+RwLock+imbl, RoaringBitmap label index, TypedIndex, `IndexProvider` trait, `Mutator` write funnel, `GraphTypeDef` runtime binding. |
| `selene-persist` | core | WAL (`SLDB` magic) + snapshot (`SLSN` TLV-tagged sections) + recovery. **Never sees `Graph`** — takes `&[Change]`, returns `RecoveryResult`. |
| `selene-gql` | core, graph | ISO GQL parser (pest), AST, semantic analyzer, planner, optimizer, executor, `ProcedureRegistry` trait. |
| `selene-pack` (TBD) | core, graph, gql | Procedure-pack registry, validator, activation state machine, native procedure runtime. |
| `selene-algorithms` (TBD) | core, graph | Graph algorithms (PageRank, WCC, SCC, Dijkstra, etc.); independent of GQL. |
| `selene-testing` | core, graph | Test fixtures and synthetic graph generators. |

Opt-in extension crates depend on the mandatory crates plus the procedure-pack/index hooks: `selene-vector` (HNSW + vector procedures, D5); future `selene-timeseries`, `selene-rdf`, `selene-graphrag`, `selene-fulltext`. **No umbrella crate.** Crate boundaries are enforced by code review and `cargo-deny`.

## Decision log

All v1.0 architecture is settled. Canonical text lives in the linked spec section; this table is the breadcrumb index. Fields named in a row reflect the *current* shape after any amendments.

| ID | Decision | Canonical home |
|---|---|---|
| D1 | v1.0 = embeddable library only (no server, no transport, no auth, no MCP) | `_spec/01` §4 |
| D2 | Conformance: minimum + ~30–40 parser-reachable optional features | `_spec/01` §5 |
| D3 | Schema: both GG01 (open) and GG02 (closed); per-graph choice | `_spec/02` §6 |
| D4 | Procedure-pack extension model + feature-gated workspace modules | `_spec/05` §3, §5 |
| D5 | Vector via `Value::Extended`; HNSW lives in `selene-vector` | `_spec/02` §3, `_spec/06` §3 |
| D6 | WAL+snapshot from day one in `selene-persist` | `_spec/04` |
| D7 | Concurrency: `ArcSwap` + `parking_lot::RwLock` + `imbl` CoW; `imbl` MPL-2.0 exception accepted | `_spec/03` §4 |
| D8 | Multi-crate, no umbrella; linear dep direction | this file (Crate map), `_spec/03` §8 |
| D9 | Workspace dependency choices (imbl, rkyv, postcard, blake3, jiff, …) | `_spec/09` §3 |
| D10 | Transaction isolation: single graph write-lock + lock-free reads (strict-serializable) | `_spec/03` §6 |
| D11 | ID allocation: aborted-tx IDs become permanent holes | `_spec/02` §4, `_spec/03` §6 |
| D12 | Audit: opaque caller-supplied principal byte-slot in WAL header | `_spec/04` §3 |
| D13 | Snapshot section tag: 8-byte composite (provider + sub-tag); 24-byte row | `_spec/04` §4 |
| D14 | Snapshot serialization: `rkyv` archive over sorted-vec intermediates | `_spec/04` §4 |
| D15 | Recovery: two-step (snapshot apply → WAL replay); `RecoveryProvider` lives in `selene-persist`, `IndexProvider` lives in `selene-graph`; both use `&self` receivers | `_spec/06` §3, §12 |
| D16 | `ProcedureRegistry` trait owned by `selene-gql`; `lookup` takes `&[IStr]`; embedder injects `&dyn ProcedureRegistry` | `_spec/08` §7 |
| D17 | Procedure tiers: per-tier concrete `Context` structs + per-tier dyn-compatible `Procedure` traits | `_spec/05` §3 |
| D18 | Pack lifecycle audit: WAL-only via the same mutation funnel; no parallel ledger | `_spec/05` §5 |
| D19 | GG02 catalog: per-graph immutable `bound_type: Option<Arc<GraphTypeDef>>` runtime binding, persisted via `CORE/GTYP` | `_spec/02` §6, `_spec/03` §3 |

## Milestone log

- **M5b closed (2026-05-09)** — analyzer entry: `analyze(stmt, &registry, schema)` produces binding scopes, expression type cells, procedure/YIELD typing, statement category, mutation write sets, and statically decidable closed-graph schema errors. Runtime `selene_graph::type_validator` remains the commit-time backstop.
- **M5c opened (2026-05-09)** — lifts `AnalyzedStatement` into an optimized `ExecutionPlan`. Spec 13 (`_spec/13-iso-gql-planner.md`, local-only mirror) records implementation invariants. BRIEF-26 landed Plan IR + read-pipeline lowering; BRIEF-27 lowers mutations/DDL/CALL/transactions; BRIEFs 28–29 add the optimizer; BRIEF-30 closes M5c with the plan snapshot harness.
- **M5c progress 2/5 (2026-05-09)** — BRIEF-27 closed mutation, DDL, CALL, and transaction-control lowering. Planner output is now complete for every v1.0-claimed statement shape (no `NotImplemented` hatches at the top-level dispatch). Adds 5 new `PlannerError` variants (`SLENE_P_013..017`) that defend against analyze→plan registry drift and interner cap exhaustion. BRIEFs 28–29 add the optimizer next.
- **M5c progress 3/5 (2026-05-09)** — BRIEF-28 closed the optimizer framework + 5 structural rules (ConstantFolding, AndSplitting, FilterPushdown, ExpandFilterPushdown, TopK). `Rule` trait is infallible; `OptimizeContext` carries `EdgeStatistics` + `WanderJoinSampler` skeletons (cost-aware activation in BRIEF-29). `walk_expand_nodes` skips `JoinTree::Outer.right` to preserve post-OPTIONAL FILTER semantics. `#[non_exhaustive]` on `OptimizeContext`/`EdgeStatistics`/`PropertyHistogram` reserves room for BRIEF-29's `IndexCatalog` + selectivity hooks. BRIEF-29 lands the remaining 8 rules next.
- **M5c progress 4/5 (2026-05-09)** — BRIEF-29 closed the remaining 8 optimizer rules to complete the v1.0 13-rule set: `NodeFilterExtraction`, `CompositeIndexLookup` (with subset matching via Gosper's hack), `InListOptimization`, `RangeIndexScan` (with tightest-bound merging + contradiction detection), `IndexOrder`, `PredicateReorder`, `WcoJoin` (marker-only; AGM math is M5d Phase B), `SymmetryBreaking`. Adds `IndexCatalog` trait (D16-mirror, embedder-injected) with `IndexTarget::{Node, Edge}` and opaque `IndexHandle`; `ScanAccess` / `OrderAccess` / `TypedIndexBounds` / `NodeIdOrdering` IR additions in `plan/ir/access.rs`; `ExecutionPlan.next_expr_id` allocator; `#[non_exhaustive]` on `JoinTree`, `ImplDefinedCaps`, `NodeIdOrdering`. v1.0 edge typed/composite indexes always return `Linear` (selene-graph's typed property index is node-only — embedder concern for v1.x). BRIEF-30 closes M5c with the plan snapshot harness next.
- **M5c CLOSED (2026-05-09)** — BRIEF-30 (PR #34, merge `f6590a1`) landed the plan snapshot corpus harness behind a `test-harness` feature flag (`[[test]] required-features = ["test-harness"]` so the snapshot test isolates from `cargo test --workspace --no-default-features`). Adds `selene_testing::PlanCorpus` (18 entries × stable slug, category, expected rules; `PlanCorpusRegistry::{Empty, StandardMock}` selector) + `optimize_summary` rendering layer (`PlanSnapshot` / `PatternSnapshot` / `ScanSnapshot` / `PipelineOpSummary`, gated `#[cfg(any(test, feature = "test-harness"))]`). Three RULE_NAMES drift checks at distinct layers (registry-internal, corpus-known, snapshot-fired) make the system self-policing. Three type-preservation tests cover surviving expression rows, synthesized-predicate booleanness (using `analyzed.expr_types.len()` as the high-water mark for ExprId allocator coverage), and binding-ref sortedness/dedupe/pattern-resolvability. Spec 13 §7 (10 subsections) records optimizer implementation invariants in the local-only `_spec/13-iso-gql-planner.md` mirror. Rolled-in scope: lexer-aware parser nesting guard at depth 64 (`parser/guard.rs`) with `ParserError::NestingLimitExceeded` (SLENE_GQL_54000) — resolves a parse-fuzz timeout artifact found during validation; covered by `dos_guard.rs` (rejects over-nested input, ignores delimiters in strings/comments). M5c's planner+optimizer is now a closed, snapshot-protected surface; M5d (executor) opens next.
- **Cross-cutting infra (2026-05-10)** — INFRA-04 (PR #39, merge `3696425`) swapped `mimalloc 0.1` as the global allocator for all 12 bench binaries (5 selene-graph + 3 selene-persist + 4 selene-gql). **Library-vs-bench distinction (load-bearing)**: mimalloc is `[dev-dependencies]` only on the three library crates (`selene-graph` / `selene-persist` / `selene-gql`); embedders ship the libraries into their own binaries and pick their own allocator. New defensive CI lint `.github/scripts/check-mimalloc-dev-dep.sh` (with `.test.sh` companion covering both PASS + FAIL paths) parses each library `Cargo.toml` and fails if `mimalloc` ever appears outside `[dev-dependencies]` — closes the §G.4 footgun by construction. `about.toml` gained a `[libmimalloc-sys.clarify]` block pinning the bundled C allocator's MIT license so `THIRDPARTY.md` generation is deterministic across macOS/Linux (different `cargo-about` discovery paths). Per direction, real iai-callgrind capture deferred non-gating; baseline files retain zero placeholders with header noting allocator + refresh procedure (`scripts/run-benches.sh --layer iai --profile quick`). Codex auto-review clean (👍 verdict, no inline findings). Sequenced after BRIEFs 32+33 (PR #37 `d31e06e`, PR #38 `504bcec`) so when real numbers are captured they reflect the full Phase A executor surface. Future BRIEFs 34–40 will cause incremental drift in instruction counts but won't require a full re-baseline pass — each PR can update specific gates as needed.
- **Cross-cutting infra (2026-05-09)** — INFRA-03 (PR #35, merge `31684f6`) landed the Phase 1 benchmark harness: `criterion 0.8` + `iai-callgrind 0.16` two-layer setup, `SELENE_BENCH_PROFILE` envelopes (`quick`=1k, `full`=1k/10k/50k, `stress`=+100k), deterministic arithmetic-generator `BenchFixture` (no RNG dep), and bench coverage for `selene-graph` (M4-close: node fetch / label index / TypedIndex point+range / composite-proxy / edge cascade / mutation batches / BFS depths / concurrent reads), `selene-persist` (M3-close: WAL append+replay / snapshot read+write / full recovery), and `selene-gql` plan-only (M5c artifact: parse / analyze / plan+optimize / IR clone, consumes `PlanCorpus`). **Local-only execution** — no `.github/workflows/bench.yml`; `scripts/run-benches.sh` is the sole sanctioned entry point with sequential discipline (static `BENCHES` manifest, `pgrep` self-conflict abort, valgrind detection). Two cheap shape lints (`check-bench-invocation.sh`, `check-iai-baselines.sh`) run inside the regular CI test job to verify file shape without executing benches. Initial baselines committed as zeroed placeholders (`benches/baselines/*.iai.txt`); first real local run on a Linux/valgrind box populates them. Phase 2 (executor end-to-end benches) opens at M5d-close; Phase 3 (LDBC SNB SF0.1/SF1) at M5e-close.
- **M5d opened (2026-05-09)** — executor (Phase A row-at-a-time Volcano per spec 08 §5–§8). Spec 14 (`_spec/14-iso-gql-executor.md`, local-only mirror) records implementation invariants — same role spec 13 plays for M5c. Proposed brief sequence (BRIEFs 31–40): scaffold + scan access paths → join shapes → simple pipeline ops → OrderBy/TopK/GroupBy/aggregates → Union/Chain → mutations → catalog DDL → TX control + statement-level dispatch → CALL + per-tier Context → executor snapshot harness (M5d closure). Phase B (Free Join + COLT for cyclic, factorized representations, plan caching) is a separate later milestone. v1.0 ships with `EmptyProcedureRegistry` as the only `ProcedureRegistry` implementer until `selene-pack` is built; M5d's executor must work cleanly with `EmptyProcedureRegistry` for non-CALL statements (E11).
- **M5d progress 1/10 (2026-05-09)** — BRIEF-31 (PR #36, merge `75544af`) landed the executor scaffold + 5 scan access paths in `crates/selene-gql/src/runtime/`: `TxContext` (read-only `Arc<SeleneGraph>` snapshot per spec 14 §7.1; `WriteTxn` integration is BRIEF-36), `BindingTable` + `Binding` (`SmallVec<[Value; 8]>` row-at-a-time), `ExecutorError` (3 starter variants: `DataException` / `InvalidReference` / `ImplementationDefined`; later briefs extend via `#[non_exhaustive]`), expression evaluator (residual-filter surface; out-of-scope variants like `FunctionCall`/`CaseWhen`/`Aggregate`/`Exists` return `ImplementationDefined` explicitly — never silent `Value::Null`), and `scan_pattern` for all 5 `ScanAccess` variants (Linear / LabelIndex / TypedIndexRange / BitmapUnion / CompositeLookup). Mid-implementation IR addition: `property: IStr` added to `TypedIndexRange` and `BitmapUnion` so the executor can call `nodes_with_property_eq(label, property, value)` from the opaque `IndexHandle` (D16 boundary). Codex auto-review caught two P1 findings around `f64`-coercion losing `i64` precision above 2^53; fix commit `143b876` extracted shared `runtime/value_compare.rs` with type-preserving comparison (lossless cross-type promotion via exact-representability bit checks). Spec 14 §7 gained E17 invariant: numeric comparison preserves integer precision. Three-valued logic (`NULL = NULL → NULL`, `false AND NULL → false`, etc.) explicitly tested to guard against the donor's `NULL = NULL → true` bug. BRIEF-32 (Expand / HashJoin / Outer / Subplan / WCO marker unwrap; `execute_pattern` orchestrator) opens next.
- **M5d progress 2/10 (2026-05-10)** — BRIEF-32 (PR #37, merge `d31e06e`) landed the pattern-executor join shapes in `crates/selene-gql/src/runtime/`: `pattern.rs` (`execute_pattern` orchestrator + `walk_join_tree` recursive dispatch), `expand.rs` (3-direction edge traversal + `BTreeSet`-based self-loop dedup for `EdgeDirection::Undirected`), `hash_join.rs` (planner-chosen `BuildSide` strictly honored; key equality via `value_compare::equal_non_null` carry-forward from BRIEF-31), `outer.rs` (left-outer with right-side parameterization via `WalkContext.seed`), `subplan.rs` (`ExecutionPlan` recursion sharing the parent `TxContext`), `wco.rs` (Phase A unwrap of `intersection[0]` with defensive `ImplementationDefined` for empty/multi-branch). `scan_pattern` demoted to `pub(crate)`; `execute_pattern` is the public pattern entry. IR additions: `BuildSide::{Left, Right}` on `JoinTree::HashJoin`; `HiddenBindingId(u32)` + `hidden_binding`/`left_hidden_binding`/`right_hidden_binding` fields on `NodeOrEdgeScan` and `EdgeMatch` (executor-private slots for anonymous pattern elements, enabling chained anonymous expansions like `(a)-[:R]->()-[:S]->(b)`). `JoinTree::Outer` gained `right_filters: Vec<FilterPredicate>` to scope OPTIONAL MATCH WHERE predicates inside the join tree (preserves OPTIONAL semantics; previously over-applied at orchestrator scope). Rolled-in scope: dead `TypedIndexBounds::InList` variant + 2 executor match arms removed (BRIEF-31 N1 cleanup). Mid-implementation correctness fix surfaced (§O C8): `pattern.filters` extracted out of scan and applied at the orchestrator after `walk_join_tree` materializes — prevented per-scan re-application under Expand/Outer/WCO. Codex auto-review caught 4 P1 findings (OPTIONAL filter timing, anonymous Expand chain, two latent subplan gaps) + 1 P2 (hash join nested-loop, deferred to perf-driven follow-up); fix commit `0912799` resolved all 4 P1s with regression tests (`outer_filter_on_optional_binding_keeps_unmatched_left_rows`, `expand_chains_through_anonymous_intermediate_node`, two subplan guards). Spec 14 §1 amendment 1 (2026-05-09) sharpened Phase B scope to include factorized binding tables + adaptive re-optimization; §7.8 added E18 (TxContext extensibility for adaptive re-opt — BRIEF-33 carries the `reopt_hook: Option<&dyn AdaptiveOptimizer>` field). Self-review missed P1.1 (filter-placement under OPTIONAL); recorded as `feedback_filter_placement_audit` for future audit discipline. BRIEF-33 (pipeline ops: Filter / Project / Let / Unwind / Limit / Distinct + `execute_pipeline` entry) opens next.
- **M5d progress 3/10 (2026-05-10)** — BRIEF-33 (PR #38, merge `504bcec`) landed the pipeline executor in `crates/selene-gql/src/runtime/pipeline/`: `mod.rs` (`execute_pipeline` orchestrator + dispatch over `PipelineOp` variants; defensive `ImplementationDefined` for the 9 out-of-scope variants pending BRIEFs 34–39), `filter.rs` (three-valued logic per E7: only `Value::Bool(true)` passes; defensive on `index_consumed = true` per E1), `project.rs` (schema replacement; per-row expression evaluation), `let_op.rs` (**progressive left-to-right cumulative scope** — earlier items visible to later items per ISO GQL LET semantics; landed via fix commit after Codex caught the original eval-against-original-row bug), `unwind.rs` (list → rows; NULL → 0 rows per Cypher convention; non-list → `DataException` with span per ISO 14.7), `limit.rs` (`LimitAmount::Literal(u64)` evaluated once before slice; saturating arithmetic; `Parameter` defensive `ImplementationDefined`), `distinct.rs` (typed dedup via `mem::discriminant` + `value_compare::equal_non_null`; ISO 9.20 `Int(1) ≠ Float(1.0)` enforced; linear-scan O(N²) Phase A simplification paired with BRIEF-32 hash join P2 deferral). **E18 landed** per spec 14 §7.8: `AdaptiveOptimizer` trait (no-op default `observe_cardinality`) + `PipelineOpId` newtype + `TxContext.reopt_hook: Option<&dyn AdaptiveOptimizer>` field + `read_only_with_reopt(...)` constructor. `execute_pipeline` takes `&TxContext` (not `&mut`) per Q11 lean — Phase B activation will move to interior mutability inside the trait impl. Codex auto-review caught 1 P1 (LET progressive eval — items evaluated against unmodified row + schema, breaking `LET a = 1, b = a + 1`) + 3 P2 (`PipelineOpId` high-water mark hygiene — set once at lowering, never refreshed against NEXT chains, optimizer rewrites, or nested `Subplan` ExecutionPlans inside JoinTrees); fix commit `1536c33` resolved with new `ExecutionPlan::refresh_pipeline_op_high_water()` helper that recurses into `Union`/`Chain` rhs subplans AND `pattern_plan.join_tree`'s `Subplan` variant (Codex went beyond my fix memo to catch the join-tree-nested case I missed). Called from end of `lowering::plan(...)` and after both convergence + single-pass paths in `optimize_with_rules`. Regression tests: `let_evaluates_items_in_order_with_progressive_visibility`, `composite_plan_refreshes_next_pipeline_op_id_after_union_append`, `chained_plan_refreshes_next_pipeline_op_id_after_chain_append`, `optimizer_refreshes_pipeline_op_id_high_water_after_rewrite`. Self-review missed P1 LET progressive eval (single-item LET tests passed; multi-item cumulative scope semantics wasn't pinned). BRIEF-34 (OrderBy / TopK / GroupBy / aggregates) opens next.
- **M5d progress 4/10 (2026-05-10)** — BRIEF-34 (PR #40, merge `85f570e`) landed OrderBy / TopK / GroupBy + 7 aggregates (Count / CountStar / Sum / Avg / Min / Max / Collect) in `crates/selene-gql/src/runtime/pipeline/`: `order_by.rs` (stable `slice::sort_by` per E8), `top_k.rs` (`BinaryHeap<Reverse<...>>` partial sort, equivalent to OrderBy + Limit), `group_by.rs` (linear-scan group-key matching using `value_compare::equal_non_null` for lossless cross-type numeric equality; implicit-group emits 1 row on empty input, explicit-group emits 0), `aggregate.rs` (per-aggregate `AggregateSlot` with E8 empty-input semantics: `Sum→Int(0)`, `Avg→Null` not div-by-zero — the donor regression guard `avg_empty_group_returns_null_not_division_by_zero`, `Sum` integer overflow → `DataException`). `value_compare::compare_for_sort` + `NullSortOrder` enum land as `pub(crate)` for OrderBy NULL placement. **Aggregate identity is ExprId-anchored** — Codex auto-review caught the load-bearing P1 that aggregate columns were resolved by function name in both `evaluator::aggregate_column_value` and `project::aggregate_column_value`; with two `sum(...)` calls in one RETURN both projections read the first matching `sum` column. Fix commit `3291a0b` introduces a new `crates/selene-gql/src/plan/lowering/aggregate.rs` module that synthesizes a per-aggregate column name (`agg_<expr_id>`, interned via `intern_with_admission` with `PlannerError::InternerCapExhausted` on cap-exhaust) and rewrites every aggregate `FunctionCall` in surviving Project items + HAVING predicate into `Variable { name: agg_<id> }` via `rewrite_aggregate_refs` — name-based lookup is gone everywhere (`evaluator` reverts to `ImplementationDefined` on `FunctionCall`, `project` becomes a single `evaluator::evaluate` call). Codex's same review batch also surfaced 1 distinct P1 (`compare_non_null_for_sort` fell through to `value_rank`-only for same-variant pairs of unhandled types, leaving ORDER BY by Date/UUID/Bytes/NodeRef/Decimal/Int128/Uint128 effectively unsorted) + 2 P2 (DISTINCT discriminant gate skipped cross-type numeric equivalence — `count(DISTINCT [1, 1.0])` returned 2; LET-shadow case where `LET sum = 5 ... RETURN sum(x)` silently read the LET column — resolved structurally by the aggregate-identity fix). All resolved in `3291a0b` with regression tests `multiple_sum_aggregates_with_distinct_arguments_disambiguate`, `unaliased_duplicate_aggregate_functions_disambiguate_by_position`, `having_unaliased_aggregate_disambiguates_from_returned_aggregate`, `count_distinct_dedups_cross_type_numeric_equivalents`, `collect_distinct_dedups_cross_type_numeric_equivalents`, `function_call_with_let_shadow_does_not_misread_column` (gated on `test-harness` feature via new `runtime::evaluate_for_test` re-export), plus per-variant unit tests in `value_compare::tests` for Date/LocalDateTime/ZonedDateTime/LocalTime/ZonedTime/Duration (via deterministic field-tuple `duration_key`)/Bytes/Uuid/NodeRef/EdgeRef/Decimal/Int128/Uint128. Self-review caught nothing material before Codex; lesson: future briefs that introduce post-grouped expression resolution must include same-function-name-twice tests at the start, not the end. After this brief: every read statement that includes ORDER BY / `RETURN COUNT(*)` / `RETURN GROUP BY ... AGG(...)` is executable end-to-end. Combined with BRIEF-33's Filter / Project / Let / Unwind / Limit / Distinct, the pipeline executor covers every Phase A read operator EXCEPT Union / Chain (BRIEF-35) and CALL (BRIEF-39). BRIEF-35 (Union / Chain) opens next.
- **M5d progress 5/10 (2026-05-10)** — BRIEF-35 (PR #41, merge `39ee94a`) landed `PipelineOp::Union` + `PipelineOp::Chain` execution in `crates/selene-gql/src/runtime/`: `plan_runner.rs` (`pub(crate) fn execute_plan(plan, ctx)` standalone-plan entrypoint + `seed_table()` helper for pattern-less plans like `RETURN 1` standalone; CALL in BRIEF-39 will reuse), `pipeline/union.rs` (Phase A handles `SetOp::Union` concat+distinct + `SetOp::UnionAll` concat; the other 5 SetOp variants — `Intersect / IntersectAll / Except / ExceptAll / Otherwise` — return `ImplementationDefined` per Q2 scope reduction; lhs schema is canonical with column-count `DataException` defense-in-depth), `pipeline/chain.rs` (5-line "discard input table" design per BRIEF-26 §O.C6). **Correlated NEXT is rejected at plan time as Phase A scope reduction** — `plan/lowering.rs::assert_no_correlated_next` walks `analyzed.references` for each NEXT block; any reference whose declaration span sits outside the block's span surfaces as `PlannerError::NotImplemented { feature: "correlated NEXT (RHS references prior-block bindings)" }`. Span-based detection (uses `BindingScopeTree::declaration` + span containment, no new analyzer surface) is immune to the name-shadow / subquery-local / pattern_plan-guard bug classes that broke Codex's initial heuristic. Codex auto-review caught 5 P1 findings on the initial commit's chain.rs correlation heuristic (`binding_refs.is_empty()` proxy treating subquery-local bindings as incoming refs; name-based `expr_references_schema` shadow-vulnerable; `pattern_plan.is_none()` guard always losing correlation when MATCH present; Union-inside-chain dropping carried bindings — finding #5 made moot by the structural fix). All resolved in `96fc7c2` by reverting chain.rs to the brief's design and adding the planner-time guard. Regression test `correlated_next_returns_planner_not_implemented` pins the planner error; `chain_rhs_sees_same_snapshot_as_lhs` updated to use distinct binding names per block (avoiding the analyzer's `boundary=false` cross-block binding reuse). **Lesson recorded as `feedback_brief_cite_verification` memory**: when a brief cites a prior brief's §O resolution as load-bearing (here BRIEF-26 §O.C6 said "NEXT establishes a fresh binding scope" but the analyzer's `bind/mod.rs:44-61` chains scopes with `boundary=false` so projection aliases flow forward), verify against the in-tree code before treating the cite as ground truth — briefs decay; code is canonical. Self-review missed this because I trusted the cite without checking the analyzer; Codex caught the contradiction empirically. Phase B work: full correlated NEXT execution + the 5 deferred SetOp variants. After this brief: every Phase A read operator EXCEPT CALL (BRIEF-39) executes end-to-end. BRIEF-36 (mutation execution) opens next.
- **M5d progress 6/10 (2026-05-10)** — BRIEF-36 (PR #42, merge `dc8fa5f`) landed mutation execution — the **first write-side brief** in M5d. Files: `crates/selene-gql/src/runtime/pipeline/mutation.rs` (per-variant dispatch over the 7 `MutationOp` variants — `InsertNode / InsertEdge / SetProperty / SetLabel / RemoveProperty / RemoveLabel / DeleteTarget`; row-at-a-time per spec 14 §5.8; routes through `ctx.mutator()?.create_node | create_edge | update_node | update_edge | delete_node | delete_edge` per E12 / hard rule 11; `node_labels` enforces Phase A `LabelExpr::Single`-only with And/Or/Negation → `ImplementationDefined`; Strict DELETE on a node with incident edges errors with `DataException` per ISO clause 12.18; DETACH delete cascades via `mutator.delete_node`). `runtime/context.rs` extends `TxContext<'a>` to `TxContext<'a, 'g>` (second lifetime for `WriteTxn<'g>`); new `write(snapshot, caps, txn)` constructor + `mutator()` accessor returning `Result<Mutator, ExecutorError::InvalidTransactionState>` + working-graph-aware `snapshot()` so intra-statement reads see writes (test `match_after_insert_in_same_statement_sees_inserted_node` pins this). `runtime/error.rs` gains `InvalidTransactionState { detail, span }` (GQLSTATUS 25000) + `GraphMutation { source: GraphError, span }` (preserves underlying graph error discriminant). `plan/ir/mutation.rs` applies BRIEF-34 lesson — `output_column_index: Option<u32>` + `output_column: Option<BindingTableColumn>` on `InsertNode`/`InsertEdge`, `target_column_index: u32` on Set/Remove/Delete variants, AND `InsertEndpointRef::Binding { binding, column_index }` (Codex generalized the lesson to edge endpoints, beyond what the brief asked). `plan/lowering/mutation.rs` does **two-pass insert lowering** (all nodes first, then edges) so edge endpoint refs resolve against fully-populated `visible` schema; `push_insert_output` + `mutation_target_column_index` helpers. `selene-graph/src/write_txn.rs` gains `WriteTxn::read()` accessor; `selene-graph/src/graph.rs` gains `SeleneGraph::node_has_incident_edges` for Strict-DELETE pre-check. **`execute_pipeline` and every per-op `execute` cascade to `&mut TxContext<'_, '_>`** — mechanical signature change touching 14 runtime files (filter, project, let_op, unwind, order_by, top_k, group_by, distinct, union, chain, mutation, subplan, expand, hash_join, outer, pattern, scan, wco, plan_runner, evaluator). Per-row borrow pattern: `ctx.mutator()?` borrowed in inner block + dropped before next per-row iteration (avoids `&mut ctx` overlap with `&ctx` reborrows in evaluator). Defensive guard in `extend_schema` requires `output_column_index == schema.columns.len()` (append position only). Tests: 23 mutation tests covering E12 (smoke via `WriteTxn.changes` count), E13 (output column ordering across alternating named/anonymous INSERTs), `chain_insert_with_anonymous_middle_node_links_correctly` (per-row `insert_sites` SmallVec carry on `Binding`), Strict vs DETACH delete, idempotent SET/REMOVE, intra-statement read consistency, `mutator_error_surfaces_as_graph_mutation_executor_error`. **Codex auto-review: 👍 clean verdict, no inline findings** — first M5d brief to land without any review-round corrections; Codex's design choices (output_column on IR, target_column_index on all variants, two-pass insert lowering, `InsertEndpointRef::Binding { binding, column_index }`) consistently went beyond the brief in the right direction, applying BRIEF-34's identity-by-index lesson more thoroughly than the brief asked. Self-review noted one P3-class fragility: `binding_column_index` in lowering uses NAME-based lookup against `visible` (not exploitable today per analyzer scoping but structurally still the BRIEF-34 anti-pattern at the planner level; cleaner fix would be `BindingTableColumn { name, ty, hidden, binding_id: Option<BindingId> }`). Deferred — not blocking. After this brief: every Phase A read+write operator EXCEPT Catalog DDL (BRIEF-37), TX control (BRIEF-38), and CALL (BRIEF-39) executes end-to-end. BRIEF-37 (catalog DDL) opens next.
- **M5d progress 7/10 (2026-05-10)** — BRIEF-37 (PR #43, merge `ce89d2b`) landed catalog DDL execution — the **first cross-crate brief** in M5d (3 crates, ~1.5K LOC, 14 files). Files: `crates/selene-graph/src/mutator/catalog.rs` (4 catalog methods: `create_node_type` / `create_edge_type` / `drop_node_type` / `drop_edge_type`; atomic per-op validation via `validate_ref` BEFORE write-back to `bound_type`; emits `Change::SchemaChanged` per E12; split into submodule per C11 to keep `mutator.rs` under 700 LOC), `mutator/catalog/tests.rs` (8 unit tests), `selene-core/src/changeset.rs` adds `SchemaChange::NodeTypeDropped { graph_type, name }` + `EdgeTypeDropped { graph_type, name }` per Q4, `selene-graph/src/graph_types.rs` gains `node_type_index_for / without_node_type / without_edge_type` helpers, `selene-graph/src/graph.rs` gains `SeleneGraph::graph_id()` accessor (Q1), `selene-graph/src/write_txn.rs` adds commit-time `validate_entity_state` walk when changeset contains any `Change::SchemaChanged` (closes C3 gap — `type_validator::validate_change` was a no-op for SchemaChanged; preserves the cheap path when no schema change is present), `selene-gql/src/runtime/pipeline/catalog.rs` (per-variant dispatch over the 8 `CatalogOp` variants; SHOW NODE/EDGE TYPES read from `ctx.snapshot().bound_type()` and emit parser-style DDL per C10; CREATE/DROP NODE/EDGE TYPE wired through Mutator; CREATE GRAPH / DROP GRAPH → `ImplementationDefined` per D1; OR REPLACE / IF NOT EXISTS / IF EXISTS / EXTENDS / VALIDATION_MODE / property constraints except NOT NULL / multi-label endpoints / Record/List/Nothing GQL types as property value types → `ImplementationDefined`, each pinned by a defensive test), `selene-gql/src/runtime/context.rs` adds `mutator_with_span(detail, span)` helper (C7) so catalog dispatch carries catalog-specific InvalidTransactionState detail + real op span. **Q12 / C1 — open graph (`bound_type = None`) refuses CREATE/DROP type DDL** with `DataException("open graph (GG01) does not support catalog type DDL — use a closed graph (GG02)")`; SHOW NODE/EDGE TYPES on open graph returns 0 rows (well-defined; reading the absence of a closed schema). **C2 / Q3** — `implicit_graph_type_id() = GraphTypeId::new(1).expect("implicit graph type id")` via local helper (zero is invalid by `GraphTypeId::new` constructor). **C4 / Q11** — `drop_node_type` refuses any drop where an edge endpoint index is `>= removed_index` (catches both "edge depends on dropped type" AND "edge would need positional reindexing"); explicit `GraphError::Inconsistent` rather than silent renumber. **C13** — CREATE/DROP catalog ops preserve incoming binding table (no schema replacement, no row drop) so a hand-built `Catalog(CreateNodeType) → Mutation(InsertNode)` plan threads the seed binding through both ops; SHOW replaces with `(label, definition)` row schema. E14 invariant test (`create_node_type_then_insert_in_same_statement_validates_against_new_type`) hand-builds the mixed DDL+DML plan since the parser/planner doesn't yet emit such statements (C12). **Codex brief-back round before implementation** caught 15 complications (C1-C15) + 13 question leans, all accepted; first brief in M5d to use a brief-back stage. Codex round-1 review caught **1 P1 + 4 distinct P2s** (12 inline comments, 6 distinct after dedup). **P1 (load-bearing): WAL replay loses catalog DDL after crash** — `RecoveryState::apply_change` no-op'd `Change::SchemaChanged`, so post-crash recovery published a `SeleneGraph` whose `bound_type` reflected the pre-WAL snapshot; closed graph divergence between committed and recovered state. Fix commit `58a9626` resolved by collecting catalog `SchemaChange` variants into a `pending_schema_changes: Vec<SchemaChange>` during WAL replay, then replaying them against `bound_type` in `into_graph` (after meta resolution; before `validate_entity_state`). New `runtime_node_type_def` / `runtime_edge_type_def` / `runtime_properties` / `runtime_value_type` helpers translate `selene_core::NodeTypeDef` ↔ `selene_graph::NodeTypeDef`. v1.0 maps every `SchemaChange.graph_type: GraphTypeId` to recovery-state index 0 (mirrors `sections.rs:232` which encodes the bound type at u32 index 0). 6 new WAL replay tests: `wal_replay_applies_node_type_added_to_graph_type` / `_edge_type_added` / `_node_type_dropped` / `_edge_type_dropped` / `_node_type_added_against_open_snapshot_returns_inconsistent` / WAL-only end-to-end recovery. Codex's defer-then-replay design (collect during apply_change, apply at into_graph) was cleaner than my memo's per-change mutation suggestion — correctly handles the WAL-only-recovery branch (no snapshot) where `self.graph_types` would be empty under per-change mutation. **P2a — txn check ordering**: CREATE EDGE TYPE resolved endpoints from `ctx.snapshot()` BEFORE `mutator_with_span(...)` write-txn check, so read-only ctx returned `DataException` instead of `InvalidTransactionState`. Fix: added `TxContext::ensure_write_txn(detail, span) -> Result<(), ExecutorError>` non-borrowing precheck; called first in CREATE EDGE TYPE (and CREATE NODE TYPE for symmetric contract); `create_edge_type_without_write_txn_returns_invalid_transaction_state` pins. **P2b — semantic labels in SHOW + renderer**: `selene_graph::NodeTypeDef.name` (internal type identifier) and `key_labels: LabelSet` (declared labels) are independent — donor pattern uses `name = "types.person"` with `key_labels = {"Person"}`. Original implementation rendered `name` everywhere, breaking C10 round-trippability for any non-trivial closed graph. Fix: SHOW row's `label` column + `render_node_type_def` use `key_labels`; SHOW row's `label` column + `render_edge_type_def` use `EdgeTypeDef.label`. PR-internal fixtures had `name == single(key_labels)` so existing tests didn't catch divergence — `show_node_types_renders_key_labels_not_internal_name` + `show_edge_types_renders_label_not_internal_name` pin via hand-built asymmetric `GraphTypeDef`. **P2c — multi-label endpoint rendering**: endpoint rendering picked only first label from `key_labels`. Fix: `render_endpoint` now joins the full `LabelSet` as colon-prefixed labels (e.g., `:Active:Person` in `LabelSet` iteration order); `show_edge_types_renders_multi_label_endpoint_labels` pins. All 12 inline comments answered with per-thread replies (6 canonical + 6 dup pointers). **Lessons recorded**: brief-back round caught 15 complications before implementation that would have been P1 review findings — the round paid for itself before a single line of code was written. Codex's recovery cite was off-crate (mutator/catalog.rs:52, the producer) but architecturally correct (recovery_state.rs:153 was the consumer); future briefs touching persistence need explicit "commit, omit snapshot, recover, assert state" acceptance tests. After this brief: every Phase A operator EXCEPT TX control (BRIEF-38) and CALL (BRIEF-39) executes end-to-end. BRIEF-38 (TX control + statement-level dispatch) opens next.
- **M5d progress 8/10 (2026-05-10)** — BRIEF-38 (PR #44, merge `e891ad4`) landed TX control + statement-level dispatch — pure selene-gql brief, 3 new files: `crates/selene-gql/src/runtime/session.rs` (69 LOC; `Session<'g>` carries `&'g SharedGraph` + `Option<WriteTxn<'g>>` + principal bytes + `aborted: bool`; `Session::new`, `with_principal`, `has_active_txn`, `is_aborted`, `abort` accessors), `runtime/statement.rs` (131 LOC; `execute_statement(plan, session, registry)` dispatches by `plan.category` — ReadOnly / DataModifying / CatalogModifying / TransactionControl; auto-commit boundary commits or rolls back the implicit `WriteTxn`), `runtime/pipeline/tx.rs` (54 LOC; per-variant `TxOp::{Start, Commit, Rollback}` dispatch over the session). 3 new ExecutorError variants: `TransactionAlreadyActive { span }` + `NoActiveTransaction { span }` + `InFailedTransaction { span }` — all GQLSTATUS 25000 family (the third is SLENE_X_25P02). Plan IR change: `ExecutionPlan.category: StatementCategory` field threaded from analyzer to executor (single dispatch source of truth). `runtime/pipeline/mod.rs::PipelineOp::Tx(_)` becomes defensive `ImplementationDefined { detail: "TX op surfaced inside execute_pipeline; should be dispatched at statement level" }` per E10's spirit (catches misuse where someone embeds a TX op inside a non-TX-control plan). Phase A scope reductions per §G: `START TRANSACTION READ ONLY` / `READ WRITE` modes ignored (always RW-capable, IE001 lazy-upgrade is Phase B); IE001 lazy `WriteTxn` upgrade is replaced by eager-acquire at START; multi-statement read-only TX clones a fresh Arc per statement (technically violates ISO §4.6 strict-serializable for explicit read-only TX, but no Phase A tests cover it); save points / nested transactions not parsed. **Codex round-1 review caught 5 findings** on the original `c08f452` — load-bearing: F1 (P1) Aborted substate was missing; explicit-TX statement errors propagated without rolling back, so a subsequent COMMIT persisted partial state. F2 (P2) read-your-writes was broken; `execute_read_only` cloned a fresh `session.graph().read()` Arc snapshot, ignoring the active `WriteTxn`'s working state. F3 (P2) DataModifying + CatalogModifying could mix freely in one TX (no preventive guard at planner-time). F4 (P3, self-review) `expect("checked active transaction above")` violated Spec 14 §6 E16 (no `expect`/`unwrap` in production paths). F5 (P2, self-review) test count gap — 14 statement + 1 TX vs the brief's ≥22+8 acceptance bar. **Codex flagged its own brief-back §K leans on review** — Q9 ("defer Aborted substate") and Q7 ("fresh Arc per statement") were Phase A simplifications that traded safety invariants for time, which is the wrong trade. The review stage caught what the brief-back stage's lean framing missed; both stages provide distinct lens shifts. Fix commit `2c725a7` resolved all 5: F1 — added `Session.aborted: bool` + `InFailedTransaction` variant (SLENE_X_25P02) + statement-level `execute_statement` gate that rejects non-TX-control plans against an aborted session; `execute_inside_explicit_tx` sets `session.aborted = true` on error; `pipeline::tx::commit` checks aborted first, rolls back the active txn, and errors with `InFailedTransaction`; `pipeline::tx::rollback` and `Session::abort` both clear the aborted flag. F2 — `execute_read_only` now branches on `session.active_txn.as_mut()`; when present, uses `TxContext::write` so MATCH inside an explicit TX sees the WriteTxn's working state (read-your-writes contract restored). F3 — deferred to Phase B with defensive test `explicit_tx_allows_data_then_catalog_mix_in_phase_a` pinning current behavior; rationale: WriteTxn::commit already validates closed-graph schema atomically (BRIEF-37 closure) so even a mixed `INSERT (n:Person) → CREATE NODE TYPE :Person` transaction either commits both or rolls back both. F4 — `expect()` replaced with `ok_or(ExecutorError::ImplementationDefined { detail: "explicit-TX path entered without active transaction" })?` (rolled into F1 fix). F5 — test counts lifted to 25 statement + 8 TX = 33 total (exceeds brief's 22+8=30 bar by 3). All 5 Codex inline threads have per-thread replies. CI 10/10 green. **Lessons recorded**: brief-back leans are not safety-invariant grants — Phase A simplifications should only trade performance / ergonomics, never correctness or atomicity contracts. The standing memory `feedback_brief_cite_verification` already covered "verify briefs against in-tree code"; this brief surfaces the dual lesson that briefs (and brief-back outputs) are guidance, not gospel — review-stage findings can and should override brief-stage leans when safety is at stake. After this brief: every Phase A operator EXCEPT CALL (BRIEF-39) executes end-to-end; embedders can now drive multi-statement explicit transactions through the `Session<'g>` API. BRIEF-39 (CALL + per-tier Context) opens next; BRIEF-40 (executor snapshot harness) closes M5d.
- **M5d progress 9/10 (2026-05-10)** — BRIEF-39 (PR #45, merge `266b11e`) landed CALL execution + per-tier Context — the **most architecturally consequential brief** in M5d, finalizing the v1.0+ procedure boundary contract that `selene-pack` will be built against. Files added: `crates/selene-gql/src/runtime/context_tiers.rs` (78 LOC; `GraphContext<'a>` + `MutationContext<'a, 'g>` + `ProcedureContext<'a, 'g>` enum dispatch wrapper, `#[non_exhaustive]` to reserve room for Phase B `Persist` variant), `runtime/pipeline/call/{mod, context, project}.rs` (62/64/120 LOC; per-row dispatch orchestrator + tier-context construction + yield projection with output type validation), `tests/exec_pipeline_call.rs` (668 LOC; **19 tests**). Files modified: `procedure_registry.rs` ~60-line refactor (drop placeholder `Value` struct → `pub use selene_core::Value`; drop unit `ProcedureResult` → `pub struct ProcedureResult { rows: Vec<Vec<Value>> }`; drop `M2Placeholder` → 5 concrete `ProcedureError` variants `UnknownProcedure / InvalidArgument / Capability / TierMismatch / Internal` with `#[non_exhaustive]` + `gqlstatus()` mapping method); `runtime/context.rs` adds `registry: &'a dyn ProcedureRegistry` field to `TxContext` per C1 (resolution: stash on `TxContext` rather than 14-file signature cascade — matches E18's "TxContext is the only execution-time state operators carry"); `plan/ir/call.rs` adds `tier: ProcedureTier` + `yield_schema: Vec<BindingTableColumn>` per C2/C6; `runtime/pipeline/mod.rs::PipelineOp::Call` arm wires to `call::execute`; `runtime/error.rs` +1 `Procedure { source, span }` variant; `crates/selene-gql/src/error.rs` adds 3 GQLSTATUS constants (UNKNOWN_PROCEDURE 42704 / INVALID_PROCEDURE_ARGUMENT 22023 / CAPABILITY_VIOLATION 28000) per C8; `crates/selene-testing/src/mock_procedure_registry.rs` updated to new execute signature with explicit tier override builder per C7. **Brief-back round before implementation** caught 12 complications (C1–C12) + amended 15 Q-recommendations — second M5d brief to use brief-back stage; lesson from BRIEF-38's skipped brief-back (Codex round-1 caught 5 findings including 2 contradicting its own brief-back leans) was load-bearing here. Codex round-1 review: **👍 clean verdict, no inline findings** — second M5d brief to land without any review-round corrections (BRIEF-36 was the first). **Spec 05 D17 amendment 1 (2026-05-10)** clarifies the registry-boundary vs procedure-author surface split: selene-gql owns the single `ProcedureRegistry` adapter trait + `ProcedureContext` enum dispatch (single trait for dependency-direction reasons); selene-pack owns split per-tier procedure-author traits internally and adapts behind its registry impl. Original D17 wording made it sound like selene-gql should own selene-pack's split traits — that would have inverted the dependency boundary. **E20 (new)** spec 14 §7 invariant: procedure dispatch is row-at-a-time, not table-at-a-time; `ProcedureResult { rows: Vec<Vec<Value>> }` shape is forward-compatible to a future `execute_batched` default-method addition. **E21 (new)** registry-authoritative tier: drift between `metadata.tier` (registry-reported) and `metadata.mutability` (planner-classified canonical mapping `Read→Graph` / `GraphWrite|SchemaWrite|Admin→Mutation`) surfaces as `ExecutorError::Procedure { source: TierMismatch }` at executor dispatch — not silently coerced. Phase A scope reductions: PersistContext deferred entirely (v1.0 has no persist-tier procedures planned; selene-persist already takes `&[Change]` per D15) — `ProcedureTier::Persist` rejected with `ImplementationDefined` (defensive test pinned); capability checking deferred (registries may still return `ProcedureError::Capability` declaratively, but executor does not enforce — selene-pack owns the policy model); per-call timeout/cancellation out of v1.0 spec; correlated-NEXT carry-forward from BRIEF-35. **C10 zero-row vs unit-row contract pinned** with 2 dedicated tests: `procedure_returning_zero_rows_drops_input_row` (`vec![]` drops the input row from cross-product) + `procedure_returning_one_unit_row_emits_one_row_per_input` (`vec![vec![]]` emits 1 row per input row with 0 columns appended; typical for no-YIELD mutation procedures). **C5 output type validation** — `project.rs::matches_gql_type` exhaustively covers every `GqlType` variant including `GqlType::Nothing → false` (rejects all values; matches spec 02 typing model); `GqlType::Null` allowed for any value; mismatch surfaces as `ProcedureError::Internal { detail: "registry returned value with wrong type for column N" }`. **C9 insert-site preservation** — `Binding::with_insert_sites` propagates per-row InsertSiteId state through CALL projection so an INSERT→CALL→INSERT chain doesn't lose anonymous-insert-site state. **C11 hand-built test plans** — for invariants the analyzer rejects (E10 mutation-tier in read-only context, E21 tier-mismatch via registry drift), tests construct `TxContext::read_only(...)` + `execute_pipeline(&plan.pipeline, seed_table(), &mut ctx)` directly to exercise executor invariants the parser/analyzer would reject. Test coverage exceeds brief's 14-test bar by 5 (19 total). After this brief: **every Phase A operator executes end-to-end**; only BRIEF-40 (executor snapshot harness, M5d closure) remains. selene-pack (when built post-M5d) consumes `ProcedureRegistry` against the v1.0+ contract this brief locked in — the next breaking change to the registry boundary is a major-version event. BRIEF-40 (executor snapshot harness + M5d closure) opens next.
- **M5d CLOSED (2026-05-10)** — BRIEF-40 (PR #46, merge `41eaabe`) landed the executor snapshot harness, mirroring BRIEF-30's M5c plan-snapshot closure. Files added: `crates/selene-gql/src/runtime/snapshot_summary.rs` (445 LOC; `executor_summary(&ExecutorSummaryInput)` deterministic-rendering layer gated `#[cfg(any(test, feature = "test-harness"))]`; `ExecutorSnapshot` / `SnapshotColumn` / `NetGraphDelta` / `RowOrderPolicy` / `ExecutorSummaryInput` types; private `PlaceholderTable` with first-seen positional `#node_N` / `#edge_N` / `#table_N` slot assignment; private `raw_value_key` + `raw_sequence_key` length-prefix sort-key composition applied uniformly to row/list/record/record_typed/path joins; private `render_value` recursive renderer with `_ => "<value::unknown>"` wildcard arm for `Value`'s `#[non_exhaustive]` non-trivial extension safety), `crates/selene-gql/tests/executor_snapshot_corpus.rs` (285 LOC; 7 drift checks: `corpus_snapshots_match`, `corpus_slugs_are_unique`, `corpus_categories_covered`, `corpus_covers_every_operator`, `corpus_output_schemas_match_runtime_tables`, `corpus_categories_match_plan_categories`, `unordered_snapshot_placeholder_assignment_is_stable_after_row_shuffle`), 21 committed insta golden snapshots under `crates/selene-gql/tests/snapshots/`, `crates/selene-testing/src/executor_corpus/{mod, fixtures, coverage}.rs` (456/152/73 LOC; per C11 split). Files modified: `crates/selene-gql/Cargo.toml` adds `test-harness` feature + `[[test]] required-features` per BRIEF-30 pattern, `crates/selene-gql/src/lib.rs` + `runtime/mod.rs` re-export `executor_summary` / `ExecutorSnapshot` / `ExecutorSummaryInput` / `RowOrderPolicy` / `NetGraphDelta` (test-harness-gated), `crates/selene-testing/src/lib.rs` re-exports `ExecutorCorpus` / `ExecutorCorpusEntry` / `ExecutorCorpusCategory` / `ExecutorCorpusProgram` / `ExecutorCorpusRegistry` / `ExecutorCorpusFixture` / `ExecutorOperator` / `PHASE_A_OPERATORS`, `crates/selene-testing/src/mock_procedure_registry.rs` adds `insert_result` / `result_for` accessors. **21 corpus entries** spanning all 6 categories (Read 12, Mutation 3, Ddl 2, Catalog 1, Call 1, Transaction 2) and 24 `PHASE_A_OPERATORS` (every op in `runtime/pattern/scan` + `runtime/pipeline/*` + statement-level dispatch). **`ExecutorCorpusProgram::{Single, Sequence, HandBuilt}`** per C5 — `Single` is a parseable statement; `Sequence` runs N statements (same `WriteTxn` / `Session`) with snapshot taken after the last statement; `HandBuilt` is a `fn() -> ExecutionPlan` for shapes the parser/planner doesn't yet emit (`ddl_create_node_type_then_insert` exercises mid-statement DDL+DML mixing per E14). **Brief-back round before implementation** caught 12 complications (C1–C12) — third M5d brief to use the brief-back stage; particularly load-bearing because my brief had **3 outright API errors** that brief-back caught: C3 claimed `Value: Display` (it's not, and `#[non_exhaustive]` requires an explicit wildcard arm in downstream renderers), C6 cited a non-existent `WriteTxn::changes()` accessor (changes only flow through `CommitOutcome` which `execute_statement` discards — resolved with pre/post `NetGraphDelta::between(pre, post)` measuring node/edge count delta + `schema_changed` flag, coarser than my proposal but sufficient for E22 byte-identity), C12 named slug `mutation_set_property_then_match` against a grammar that only permits MATCH before mutation ops, not SET...MATCH (renamed `mutation_match_set_return`). **Codex round-1 review caught 2 P2 findings**: F1 list sort-key ambiguity (`raw_value_key`'s `Value::List` arm joined elements with bare `,` — but `Value::String` keys serialize as `string:<text>` and the text can itself contain commas; `["a", "b,string:c"]` and `["a,string:b", "c"]` collapsed to identical sort keys, breaking E22 determinism in `RowOrderPolicy::SortDeterministic` mode where ties fall back to non-deterministic emission order); F2 hardcoded `ProcedureHandle::new(1)` in `ExecutorCorpus::standard_mock_registry` literal-coupled to `PlanCorpus`'s internal `next_handle` allocation order (fragile under future PlanCorpus drift). Fix commit `e800da3` resolved both: F1 — extracted shared `raw_sequence_key(prefix, suffix, elements)` helper with `<len>:<element>` length-prefix encoding applied uniformly to row/list/record (open)/record_typed/path joins (Codex went beyond my fix memo to apply it to ALL composite-value sort keys, not just lists; typed-record `Option<Value>` slots additionally disambiguated as `none`/`some:<...>` to avoid a string `_` collision); F2 — `ExecutorCorpus::standard_mock_registry` now resolves the handle through `registry.lookup(&[istr("pkg"), istr("all")]).handle` and stashes results via `insert_result` (decoupled from PlanCorpus internal allocation order). One CALL snapshot row-order accepted under the new canonical form (no information lost — same rows, different lexicographic sort under length-prefix encoding where `"row:[11:string:beta..."` sorts before `"row:[12:string:alpha..."`). Regression tests pinned both: `raw_list_keys_are_injective_when_string_elements_contain_commas` (F1) + `standard_call_result_uses_lookup_handle_not_raw_literal` (F2 — registers `pkg.before` first so `pkg.all` lands at handle 2, then asserts the result is stored against handle 2 not literal handle 1). All 3 Codex inline threads have per-thread replies (2 F1 dupes + 1 F2). **Spec 14 §12 amendment 8 (2026-05-10)** records E22 (snapshot determinism: positional placeholders assigned AFTER row sort, sort-key composition injective on `Vec<Value>` via length-prefix encoding) + E23 (operator coverage: every Phase A op in `PHASE_A_OPERATORS` must be covered by a corpus entry, enforced by `corpus_covers_every_operator` drift check). Spec 14 status updated from "Active for milestone M5d" to "M5d CLOSED (2026-05-10)". **Lessons recorded** as `feedback_loc_budget_per_file_only` (the only LOC cap that gates merges is **700 LOC per file**; per-crate budgets in CLAUDE.md hard rule 6 are guidance for splitting decisions, NOT acceptance-bar gates — Codex burned ~10 minutes shaving lines to fit a "selene-gql ≤ 35–45K" bar I wrote into the brief during BRIEF-40 implementation; that time was wasted because the user only cares about the 700-LOC per-file gate). **M5d CLOSED**: every Phase A operator (BRIEFs 31–40) is now both implemented AND snapshot-protected against silent drift. The next breaking change to the `ProcedureRegistry` adapter boundary or `executor_summary`'s rendering layer is a major-version event. M5e (likely `selene-pack` — building the procedure-pack registry against BRIEF-39's contract; spec 05 amendment 1 records the registry-boundary vs procedure-author surface split) opens next.
- **M5e progress 1/9 (2026-05-10)** — BRIEF-41 (PR #47, merge `9cd1cb8`) opened M5e by landing the `selene-pack` workspace crate scaffold + `ProcedurePackRegistry` implementing BRIEF-39's `ProcedureRegistry` contract + first platform built-in `selene.health`. Files added: `crates/selene-pack/Cargo.toml` (papaya 0.2 workspace dep + selene-graph as dev-dep per Claude self-review S1), `lib.rs` (re-exports the BRIEF-39 contract surface scoped to public types per Q2), `error.rs` (6 `RegistryError` variants: `Conflict`, `TierMismatch`, `InvalidName`, `PersistTierNotInV1`, `InternerCapExhausted`, `MutabilityTierMismatch` — the last added in round-1 review F3), `registry/mod.rs` (`ProcedurePackRegistry::empty()` + `::with_builtins() -> Result<Self, RegistryError>` + `pub(crate) ProcedurePackRegistryBuilder` per round-1 F4 demotion), `registry/storage.rs` (3-tier `TierEntry` enum + `PendingEntry` + `RegistryStorage` papaya-backed frozen storage + `validate_tier` checking Persist rejection + tier-attempt match + mutability-implied-tier match per round-1 F3 + `PLACEHOLDER_CONTENT_HASH` sentinel per round-1 F1), `builtin/mod.rs` (`pub(crate) BuiltInMetadata` root trait + `GraphProcedureBuiltIn` + `MutationProcedureBuiltIn` per Q8 scoping; `MutationProcedureBuiltIn` already exposed for BRIEF-42), `builtin/health.rs` (`SeleneHealth` returns `(graph_id Uint64, node_count Uint64, edge_count Uint64, schema_bound Boolean)` per round-1 F2 — was Integer i64 in original commit, narrowing broke for graph_id > i64::MAX), `tests/registry_lookup.rs` (3 public-API tests), `tests/health_call.rs` (7 end-to-end CALL tests including `selene_health_inside_explicit_tx_sees_uncommitted_inserts_via_real_registry` per Q5 amendment, `registry_execute_reports_tier_mismatch_expected_from_entry_actual_from_context` per C9, `top_level_call_with_trailing_return_stays_outside_phase_a_shape` per C6). Files deleted: `crates/selene-pack/src/contexts.rs` (90 LOC of dead placeholder scaffold that pre-dated BRIEF-39's `runtime/context_tiers.rs` — caught in brief-back C1). Files modified: workspace `Cargo.toml` (+ papaya 0.2 to `[workspace.dependencies]` + selene-pack to `[workspace.members]`), `Cargo.lock` regenerated, `THIRDPARTY.md` regenerated. **Brief-back round before implementation** caught 13 complications (C1–C13) + 10 Q-amendments. Most consequential: 3 outright API errors in my brief that brief-back caught before any code — C1 dead scaffold, C2 papaya not actually in workspace deps (`feedback_brief_cite_verification` lesson applied), C3 `ProcedureContext::Persist` doesn't exist (BRIEF-39 deferred Persist entirely). Plus C4 builder shape under-specified, C6 grammar (top-level CALL can't trail RETURN), C7 dev-dep cycle smell, C8 fallible interning surface, C10 `next_handle: AtomicU64` shouldn't live in frozen storage, C11 content_hash trait surface missing, C12 `SeleneGraph::default()` doesn't exist, C13 spec 15 file is gitignored. **Claude self-review** caught 2 findings on the original commit before Codex reviewed: S1 (P3) selene-graph dep should be dev-only, S2 (P2) `PlanningRegistry::health_with_signature` hardcoded `ProcedureHandle::new(1)` — same fragility class as PR #46 BRIEF-40 F2 lesson, where registration order would silently misalign plan with runtime registry as soon as BRIEF-42 lands. Fix commit `340ba26` resolved both with `runtime_health_handle(&registry)` helper that resolves through `lookup()`. **Codex round-1 review caught 5 P2 findings** on `340ba26`: F1 zero-hash duplicates silently deduped (until BRIEF-48 lands real `blake3`, identical placeholder hashes could mask different implementations — sentinel `PLACEHOLDER_CONTENT_HASH` always conflicts; identical non-placeholder hashes remain idempotent so BRIEF-48 path is forward-compatible), F2 `selene.health` narrowed `u64` IDs/counts to `i64` (false `Internal` errors for any graph_id > i64::MAX — flipped to `GqlType::Uint64` + `Value::Uint(u64)`; `i64_value` helper + overflow path deleted), F3 mutability/tier inconsistency only caught at runtime (build-time `validate_tier` now mirrors `tier_for_mutability` mapping; new `RegistryError::MutabilityTierMismatch` variant + regression test pins), F4 `ProcedurePackRegistryBuilder` advertised customization that downstream couldn't actually use (registration methods were `pub(crate)` but the struct was `pub` — demoted entire builder type to `pub(crate)`; public surface is now `empty()` + `with_builtins()` only; BRIEF-43 revisits for manifest-driven registration), F5 `with_builtins().expect(...)` panics on `InternerCapExhausted` (operational resource limit became unrecoverable crash — flipped to `Result<Self, RegistryError>` return; dropped the `Default` impl that inherited the panic). Fix commit `ecf247d` resolved all 5 with regression tests pinning each. All 5 Codex inline threads have per-thread replies pointing at the fix commit. CI 10/10 green on both fix commits. **Spec 15 §1–§4** authored locally (`_spec/15-iso-procedure-pack.md`; gitignored per CLAUDE.md underscore-folder convention) recording E24 (BRIEF-39 trait verbatim), E25 (construct-time-only), E26 (content_hash placeholder semantics), E27 (static authoring surface + fallible interning), **E28 (new — round-1 F3)** mutability/tier inconsistency caught at build, **E29 (new — round-1 F5)** public construction returns Result not panic on recoverable resource limits, **E30 (new — round-1 F2)** unsigned output values use unsigned GQL types. **Standing memory updated**: this brief is now the canonical reference for the "brief-back stage paid for itself" pattern — 13 complications including 3 outright API errors caught before code (vs review-stage which would have been a redo). New PR + merge workflow proof: user delegated PR + merge to Claude (Codex handles implementation only); Claude does self-review + Codex-review fix cycles + inline replies + merge + post-merge updates. M5d closed yesterday (2026-05-10); M5e closure is BRIEF-49. **19 selene-pack tests** (9 unit + 7 health_call + 3 registry_lookup) — exceeds §H bar of 10. BRIEF-42 (`selene.create_index` + `selene.drop_index` Mutation-tier builtins; requires IndexCatalog write API expansion in selene-graph) opens next.

## Build & test

```bash
cargo fmt --all --check
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo test --workspace --locked --all-features
cargo deny check bans licenses sources
cargo audit -d /private/tmp/selene-advisory-db
bash .github/scripts/check-file-size.sh
bash .github/scripts/check-no-secrets.sh
bash .github/scripts/check-thirdparty-current.sh
cargo +nightly fuzz run parse_gql -- -max_total_time=60

# Benchmarks: use the serialized runner only. PR CI checks invocation hygiene
# but does not execute benchmarks; run Criterion locally.
# iai-callgrind requires Linux/valgrind.
# Bench binaries use mimalloc as the global allocator; library crates are
# allocator-agnostic. See _design/perf-baselines.md §3.6.
scripts/run-benches.sh --profile quick --layer criterion
scripts/run-benches.sh --profile full --layer criterion
scripts/run-benches.sh --profile quick --layer iai

# Forbidden: workspace bench execution can run bench binaries in parallel.
# cargo bench --workspace
```

## Conventions

- **Commits:** conventional commits (`feat(scope):`, `fix(scope):`, `refactor(scope):`); scope = crate or component.
- **Branches:** trunk + `feature/*` PRs against `main`.
- **Tests with code:** every PR ships with extensive tests — units, edge cases, error paths, concurrency for shared state, property tests for invariants. Bar is "would this catch the IStr admission race."
- **Decisions over guesses:** when blocked, surface the question with options and a recommendation. Don't pre-commit.

## Working with the user

- The user is the sole author/owner of all donor codebases (no third-party license entanglements).
- Questions one at a time, with discrete options and a recommendation.
- Marathon mindset: prefer the most robust/performant/non-tech-debt path even when it touches many areas.
- This repo stays private on GitHub for the foreseeable future.

## Donor codebases (guidance only — no dependencies)

The following codebases are the user's, MIT/Apache-licensed, and are donors of design lessons and code — **not runtime dependencies**:

| Path | What it is | What we mine |
|---|---|---|
| `/Users/justin/Development/SeleneDB/` | Original prototype (~141 KLOC, 13 crates) | Battle-tested core types, pest GQL grammar, ArcSwap+RwLock concurrency, WAL/snapshot shape, RoaringBitmap label indexes. **Cautionary tales:** server bloat, HNSW inside graph crate, GQL→TS coupling, 7 critical authz bugs. |
| `/Users/justin/Development/AetherDB/` | First fork; cut RDF/TS/CLI/HTTP/MCP/federation/vault/OAuth | First scope-reduction attempt; still kept HNSW in core. |
| `/Users/justin/Development/AgentAether/aether-db/` | Second, library-only fork (7 crates, ~61 KLOC) | **Most refined extension model in the lineage.** Procedure-pack: JSON manifest, typestate-sealed activation, atomic graph-commit + post-commit audit, native `fn(&Value) -> ProcedureExecutionResult` adapters with JSON Schema gates. |
| `/Users/justin/Development/rusty-bacnet/` | Codec/parser donor (same author, MIT) | Codec patterns; surveyed 2026-05-08. |

Detailed research notes are in repo memory (`project_lessons_from_archaeology.md`, `project_donor_perf_audit.md`).

## Templates

Floor-level CI/license/lint scaffolding lives at `/Users/justin/Development/AgentAether/_templates/`:

- `ci/rust-baseline.yml` — fmt / clippy `-D warnings` / test / cargo-deny / cargo-audit / file-size cap / no-secret scan
- `ci/deny.toml` — rustls-only, MIT/Apache-2.0 + permissive transitive licenses, crates.io-only sources
- `ci/rust-toolchain.toml` — stable + rustfmt + clippy
- `ci/rustfmt.toml` — edition 2024, max_width 100
- `ci/scripts/check-file-size.sh` — 700 LOC cap
- `ci/scripts/check-no-secrets.sh` — baseline secret-pattern grep

These are the floor; per-repo additions tighten but don't weaken.

## Underscore-folder convention (working documents, not committed)

Top-level directories beginning with `_` (e.g., `_spec/`, `_briefs/`, `_review/`, `_design/`, `_plan/`) are **local-only working documents** and excluded from git via `.gitignore`'s `_*/` pattern.

Canonical durable state for design decisions, milestones, work items, and prior art lives in the **MCP graph** (selenedb instance — project node 364, label `selene-db`). The underscore-prefixed folders are dense markdown views of that state plus working scratchpads (briefs to agents, review reports, draft specs).

Implications:

- Tracked files (Cargo.toml, source under `crates/`, CLAUDE.md, LICENSE-*, NOTICE, THIRDPARTY.md, deny.toml, CI workflows, etc.) ship in git.
- Untracked working documents (`_spec/*`, `_briefs/*`, `_review/*`, `_design/*`, `_plan/*`) live on the local filesystem and are reproducible from the graph.
- When briefing an agent (Codex, etc.), specs are referenced via absolute filesystem paths, NOT relative-to-checkout paths, because a fresh `git clone` doesn't include them.
- The MCP graph is the source of truth. If a working document drifts from the graph, the graph wins; regenerate the working document.
